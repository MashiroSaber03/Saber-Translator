# ============================================
# Saber-Translator Dependencies (GPU Version - CUDA 13.0)
# ============================================
# Installation:
# 1. Install non-PyTorch dependencies first:
#    pip install -r requirements-gpu.txt
# 
# 2. If CPU-only torch was installed before, uninstall or force-reinstall it first.
#    pip uninstall -y torch torchvision
# 3. Then install GPU version of PyTorch separately:
#    pip install --no-cache-dir torch torchvision --index-url https://download.pytorch.org/whl/cu130
# 4. Reinstall the GPU runtime last. Some dependencies also declare the CPU
#    package because both distributions expose the same onnxruntime module:
#    pip install --force-reinstall --no-deps "onnxruntime-gpu>=1.27,<2"
# 5. Verify:
#    python -c "import torch, onnxruntime as ort; ort.preload_dlls(); print(torch.cuda.is_available(), ort.get_available_providers())"
# ============================================

# Flask related
flask
waitress>=3.0,<4
argon2-cffi>=25.1,<26

# Backend-first v2 persistence
SQLAlchemy>=2.0,<2.1
psutil>=7.0,<8
PySide6-Essentials>=6.8,<7

# Base tools
pyyaml
pillow
numpy
opencv-python
requests
colorama>=0.4.6,<1
packaging
networkx
shapely
pyclipper
pandas
freetype-py
fonttools
img2pdf
reportlab

# AI/OCR
openai
manga-ocr==0.1.16  # Transformers 5 tokenizer compatibility
rapidocr==3.9.2  # PP-OCRv6 ONNX runtime wrapper
onnxruntime-gpu>=1.27,<2  # CUDA 13 ONNX runtime; also includes CPU fallback
sentencepiece  # for PaddleOCR-VL
transformers==5.15.0  # native PaddleOCR-VL-1.6 runtime

# Deep Learning (torch/torchvision not included, install GPU version separately)
# torch        # Please use the command above to install GPU version
# torchvision  # Please use the command above to install GPU version
einops
ultralytics
seaborn
litelama==0.1.7
kornia==0.8.2

# Other
dill
mobi>=0.4.0
PyMuPDF>=1.23.0

# Manga Insight
chromadb>=0.4.0
httpx[http2]>=0.25.0

# Web Import
gallery-dl>=1.26.0

# Build
pyinstaller==6.19.0
