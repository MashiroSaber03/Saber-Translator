# ============================================================================
# Saber-Translator 自动构建与发布工作流
# ============================================================================
# 分支策略:
#   - main 分支: 完整功能（包含网页导入等开发功能）
#   - release 分支: 精简版（删除网页导入，用于打包发布）
#
# 触发方式:
#   1. 在 release 分支推送版本标签:
#      git checkout release
#      git tag v3.3.0
#      git push origin v3.3.0
#   2. 手动触发: GitHub Actions 页面 -> Run workflow -> 选择 release 分支
#
# 流程:
#   1. 先创建 Draft Release
#   2. 并行构建 CPU 和 GPU 版本，每个直接上传到 Release
#   3. 等待手动编辑更新日志后发布
# ============================================================================

name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: '版本号 (不带v前缀，如 3.3.0)'
        required: true
        default: '3.3.0'
      branch:
        description: '要打包的分支'
        required: true
        default: 'release'
        type: choice
        options:
          - release
          - main

env:
  MODELS_RELEASE_TAG: models-storage
  PYTHON_VERSION: '3.11'

jobs:
  # ============================================================================
  # 准备 Job - 先创建 Draft Release
  # ============================================================================
  prepare:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      version: ${{ steps.version.outputs.VERSION }}
      tag_name: ${{ steps.version.outputs.TAG_NAME }}
    
    steps:
      - name: Get version
        id: version
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
            TAG_NAME="v${VERSION}"
          else
            TAG_NAME="${{ github.ref_name }}"
            VERSION="${TAG_NAME#v}"
          fi
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "TAG_NAME=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "Version: $VERSION, Tag: $TAG_NAME"

      - name: Create Draft Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.TAG_NAME }}
          name: v${{ steps.version.outputs.VERSION }}
          body: |
            ## Saber-Translator v${{ steps.version.outputs.VERSION }}
            
            > ⚠️ **构建中...请等待 CPU 和 GPU 版本上传完成后再发布。**
            
            ### 更新日志
            
            <!-- 请在此处填写更新日志 -->
            
            ---
            
            ### 下载说明
            
            📦 **普通用户（开箱即用）：**
            - CPU 版本：下载所有 `Saber-Translator-${{ steps.version.outputs.VERSION }}-cpu.*` 文件
            - GPU 版本：下载所有 `Saber-Translator-${{ steps.version.outputs.VERSION }}-gpu.*` 文件
            - 将所有分卷放在同一目录，解压 `.zip` 文件即可
            
            🔧 **源码部署用户：**
            - 请从 [Models Storage](https://github.com/${{ github.repository }}/releases/tag/models-storage) 下载模型文件
            - 下载 `models.z01` 和 `models.zip`，解压到项目根目录的 `models/` 文件夹
            
            > 采用分卷压缩的方式，需要下载对应版本的多个压缩文件，将其放在同一个目录下解压
          draft: true
          prerelease: ${{ contains(github.ref_name, 'beta') || contains(github.ref_name, 'alpha') || contains(github.ref_name, 'rc') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ============================================================================
  # 构建 Job - CPU 和 GPU 并行构建，直接上传到 Release
  # ============================================================================
  build:
    needs: prepare
    runs-on: windows-latest
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - variant: cpu
            requirements: requirements-cpu.txt
          - variant: gpu
            requirements: requirements-gpu.txt

    steps:
      # --------------------------------------------------
      # 1. 检出代码
      # --------------------------------------------------
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.branch || github.ref }}

      # --------------------------------------------------
      # 2. 从 models-storage Release 下载模型文件
      # --------------------------------------------------
      - name: Download models from storage release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          Write-Host "Downloading models from release: ${{ env.MODELS_RELEASE_TAG }}"
          gh release download ${{ env.MODELS_RELEASE_TAG }} --pattern "models.*" --dir .
          
          Write-Host "Downloaded files:"
          Get-ChildItem -Filter "models.*" | Format-Table Name, @{N='SizeMB';E={[math]::Round($_.Length/1MB, 2)}}

      # --------------------------------------------------
      # 3. 解压模型文件
      # --------------------------------------------------
      - name: Extract models
        run: |
          Write-Host "Extracting models..."
          7z x models.zip -omodels/ -y
          
          Write-Host "Models extracted. Directory contents:"
          Get-ChildItem -Path models/ | Select-Object Name, @{N='SizeMB';E={[math]::Round($_.Length/1MB, 2)}}

      # --------------------------------------------------
      # 4. 清理模型压缩包（节省磁盘空间）
      # --------------------------------------------------
      - name: Clean up model archives
        run: |
          Write-Host "Cleaning up model archives to save disk space..."
          Remove-Item models.z01 -Force -ErrorAction SilentlyContinue
          Remove-Item models.zip -Force -ErrorAction SilentlyContinue

      # --------------------------------------------------
      # 5. 设置 Python 环境
      # --------------------------------------------------
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      # --------------------------------------------------
      # 6. 安装依赖 (CPU 和 GPU 流程不同)
      # --------------------------------------------------
      - name: Install dependencies (CPU)
        if: matrix.variant == 'cpu'
        run: |
          Write-Host "Installing CPU dependencies..."
          python -m pip install --upgrade pip
          pip install -r ${{ matrix.requirements }}

      - name: Install dependencies (GPU)
        if: matrix.variant == 'gpu'
        run: |
          Write-Host "Installing GPU dependencies..."
          python -m pip install --upgrade pip
          
          # Step 1: Install all dependencies (will install CPU torch)
          pip install -r ${{ matrix.requirements }}
          
          # Step 2: Uninstall CPU PyTorch
          Write-Host "Replacing CPU PyTorch with CUDA version..."
          pip uninstall torch torchvision torchaudio -y
          
          # Step 3: Install CUDA 13.0 PyTorch
          pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130

      # --------------------------------------------------
      # 7. 使用 PyInstaller 打包
      # --------------------------------------------------
      - name: Build with PyInstaller
        run: |
          Write-Host "Building with PyInstaller..."
          pyinstaller app.spec --noconfirm
          
          Write-Host "Build completed. Output directory:"
          Get-ChildItem -Path dist/ | Select-Object Name

      # --------------------------------------------------
      # 8. ZIP 分卷压缩（使用较低压缩级别以加快速度）
      # --------------------------------------------------
      - name: Package with ZIP split
        run: |
          $version = "${{ needs.prepare.outputs.version }}"
          $variant = "${{ matrix.variant }}"
          $outputName = "Saber-Translator-$version-$variant"
          
          Write-Host "Compressing... Output: $outputName.zip (split at 1900MB)"
          
          Push-Location dist
          
          # Use compression level 5 (faster) instead of 9 (slowest)
          # -tzip: ZIP format
          # -mx=5: Medium compression level (much faster)
          # -v1900m: Split at 1900MB
          7z a -tzip -mx=5 -v1900m "../$outputName.zip" "Saber-Translator/*"
          
          Pop-Location
          
          Write-Host "Compression completed. Generated files:"
          Get-ChildItem "$outputName.*" | Format-Table Name, @{N='SizeMB';E={[math]::Round($_.Length/1MB, 2)}}

      # --------------------------------------------------
      # 9. 重命名分卷文件（.zip.001 -> .z01 等）
      # --------------------------------------------------
      - name: Rename split archives
        run: |
          $version = "${{ needs.prepare.outputs.version }}"
          $variant = "${{ matrix.variant }}"
          $baseName = "Saber-Translator-$version-$variant"
          
          Write-Host "Renaming split archives..."
          
          $files = Get-ChildItem "$baseName.zip.*" | Sort-Object Name
          
          foreach ($file in $files) {
            if ($file.Name -match '\.zip\.(\d+)$') {
              $num = [int]$Matches[1]
              
              if ($num -eq $files.Count) {
                $newName = "$baseName.zip"
              } else {
                $newName = "$baseName.z{0:D2}" -f $num
              }
              
              Rename-Item $file.FullName $newName
              Write-Host "   $($file.Name) -> $newName"
            }
          }
          
          Write-Host "Renamed files:"
          Get-ChildItem "$baseName.*" | Where-Object { $_.Extension -match '\.(zip|z\d+)$' } | Format-Table Name, @{N='SizeMB';E={[math]::Round($_.Length/1MB, 2)}}

      # --------------------------------------------------
      # 10. 直接上传到 Release（不通过 artifacts）
      # --------------------------------------------------
      - name: Upload to Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $version = "${{ needs.prepare.outputs.version }}"
          $variant = "${{ matrix.variant }}"
          $tagName = "${{ needs.prepare.outputs.tag_name }}"
          $baseName = "Saber-Translator-$version-$variant"
          
          Write-Host "Uploading $variant version to Release $tagName..."
          
          # Get all zip/split files
          $files = Get-ChildItem "$baseName.*" | Where-Object { $_.Extension -match '\.(zip|z\d+)$' }
          
          foreach ($file in $files) {
            Write-Host "Uploading: $($file.Name) ($([math]::Round($file.Length/1MB, 0)) MB)"
            gh release upload $tagName $file.FullName --clobber
          }
          
          Write-Host "Upload completed for $variant version!"

  # ============================================================================
  # 完成 Job - 输出结果
  # ============================================================================
  complete:
    needs: [prepare, build]
    runs-on: ubuntu-latest
    if: always()
    
    steps:
      - name: Summary
        run: |
          echo "=========================================="
          echo "Build and Release Summary"
          echo "=========================================="
          echo ""
          echo "Version: ${{ needs.prepare.outputs.version }}"
          echo "Tag: ${{ needs.prepare.outputs.tag_name }}"
          echo ""
          echo "Next steps:"
          echo "  1. Go to GitHub Releases page"
          echo "  2. Edit the draft release v${{ needs.prepare.outputs.version }}"
          echo "  3. Write your changelog"
          echo "  4. Click 'Publish release'"
          echo ""
          echo "Link: https://github.com/${{ github.repository }}/releases"
