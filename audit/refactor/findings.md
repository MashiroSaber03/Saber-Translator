# 审查发现与修正日志

只有在逐文件检查中得到明确证据的问题才记录于此。问题编号格式为 `AUDIT-###`。

| 编号 | 范围 | 证据与根因 | 修正 | 验证 | 状态 |
|---|---|---|---|---|---|
| AUDIT-001 | 任务中心批次控制 | `TaskCenterDrawer` 把“全部继续”放在仅由 `queued` 任务组成的 waiting 分组内，而条件要求 `paused`/`interrupted`，入口恒不可达；后端 `continue_batch` 契约明确只接收这两种状态。 | 将继续入口放到实际含暂停/中断成员的批次分组；等待分组仍只提供置顶和取消排队成员。 | `taskCenterControls.spec.ts` 新增暂停批次可见性测试；定向 20 tests passed。 | FIXED |
| AUDIT-002 | 任务详情并发 | 快速切换任务详情或在加载旧事件时切换任务，先发后到的响应会覆盖新选择；加载失败还会留下空的选中态。 | 用单一请求版本和 `selectedDetailJobId` 约束提交，切换/关闭使旧响应失效，旧事件合并前再次核对当前任务。 | `taskCenterStore.spec.ts` 新增乱序完成测试；定向测试及全量 typecheck 通过。 | FIXED |
| AUDIT-003 | 任务中心后台请求与命令反馈 | 初始化快照失败会阻止 SSE 建连；SSE/抽屉后台刷新、取消全部排队、上下移动和加载旧事件存在未处理 Promise；三个本地历史筛选还重复请求同一完整快照，创建分析后也重复刷新。 | 初始快照失败后仍建立 SSE 并在连接时刷新；所有后台请求显式吸收瞬态失败，用户命令统一反馈；取消全部排队增加确认；删除四处重复刷新。 | 启动快照失败恢复测试、控制组件测试、typecheck 和 `git diff --check` 通过。 | FIXED |
| AUDIT-004 | 批量分析书籍切换 | 连续选择两本书时，较早的章节请求后返回会覆盖当前书籍章节。 | 以当前书籍和请求版本双重校验响应，只允许最新选择提交状态。 | `taskCenterControls.spec.ts` 新增乱序章节响应测试；定向测试通过。 | FIXED |
| AUDIT-005 | 全局确认/文本输入队列 | 两个请求队列都生成并保存从未读取的递增 `id`，没有身份或取消语义，是无效状态。 | 删除无消费者的 ID 字段与计数器，保留 FIFO 请求队列。 | 产品文本输入既有测试通过，typecheck 通过。 | FIXED |
| AUDIT-006 | 审查清单路由基线 | 清单把实际 `/reader` 与 `/insight/character-studio` 错写成 `/read`、`/character-studio`，会造成后续页面审查基线错误。 | 按当前路由、`main` 路由和路由测试统一修正清单。 | 路由源码、`main` 对照和全仓路径引用核对完成。 | FIXED |
| AUDIT-007 | 任务中心实时队列 | 前端对队列与历史都发送 `limit=200`，后端又对非终态队列直接执行该限制；超过 200 个任务后，其余任务不会出现在任务中心，因而无法查看、暂停、取消或排序。架构中的 200 上限只属于历史批次。 | 实时队列查询不再截断，`limit` 只约束历史批次；同步修正 OpenAPI 与架构文档，并增加 205 个排队任务的回归测试。 | `test_history_list_limit_counts_batches_not_member_jobs` 与 `test_queue_list_is_not_truncated_by_history_batch_limit` 均通过（2 passed）。 | FIXED |
| AUDIT-008 | 全局弹层与应用壳公共 API | `BaseModal` 的 top-end placement、compact/plain/inverse chrome、三类内部 class 透传和三类 footer 布局参数，以及 `AppShell` 的语义 variant、content scroll、fullHeight 均无生产消费者；仅有 primitive 测试构造这些参数，形成测试自证的公共契约。 | 删除无消费者的 props、计算类和 CSS 分支，保留所有真实页面使用的 typed frame/backdrop/mobile/body/size 与 shell viewport/header 契约；同步收紧 UI 规范和测试。 | BaseModal/AppShell 定向测试、typecheck、UI architecture 均通过；删除 113 行、仅增加 11 行。 | FIXED |
| AUDIT-009 | 全局 Toast | `showGeneralMessage`、字符串消息 ID、HTML Toast、按类型清理和 30 秒兼容超时没有任何生产调用，只有旧兼容测试；同时迫使全局通知保留 `v-html` 分支。 | 删除整条旧 API/数据字段/HTML 渲染和自证测试，保留当前实际使用的纯文本队列、数字 ID、定时/手动清理与四种 tone。 | Toast 单元/属性/公共组件共 13 tests passed，typecheck 与 UI architecture 通过。 | FIXED |
| AUDIT-010 | 任务中心对话框生命周期与窄屏布局 | 抽屉声明了 `role=dialog`，却没有焦点进入/约束、Esc 关闭和背景滚动锁；Windows 窄视口中 `100vw` 还会按含滚动条的 viewport 计算，使面板向左溢出约 15px。 | 抽屉接入现有对话框栈；把 BaseModal 的计数式 body 锁提取为两个真实消费者共享的 `useBodyScrollLock`；面板宽度改为遮罩包含块的 `100%`。 | 新增生命周期测试；内置浏览器 390×844 实测面板 `x=0,width=390`，焦点进入面板，Esc 关闭并恢复焦点/滚动；嵌套分析弹窗按栈关闭。 | FIXED |
| AUDIT-011 | 全局受限模式提示层级 | 后端设置失败提示使用 `popover`（1200），会覆盖任务中心和普通 BaseModal（1000），在移动端直接遮挡当前 dialog 底部。 | 状态提示改用 `mobile-overlay`（60）：仍高于页眉（40），但低于真正弹层；Toast 保持 1300。 | 内置浏览器实测状态提示 z-index 60、任务中心 1000，嵌套弹窗时不再遮挡。 | FIXED |
| AUDIT-012 | 全局文本输入弹窗 | `useDialogLifecycle` 明确只对需要输入的表单尊重显式 autofocus，但共享文本输入弹窗未声明，打开后焦点停在容器，多一次无意义键盘操作。 | 给唯一文本输入控件添加 `autofocus`。 | `productTextInput.spec.ts` 增加属性断言并通过；BaseModal 焦点测试保持通过。 | FIXED |
| AUDIT-013 | 书架列表与详情请求一致性 | 书架筛选、搜索和详情打开都允许旧请求后返回覆盖新状态；搜索组件重新挂载时也没有恢复 store 查询值，加载失败与真正空书架共用空状态。 | 列表、标签和详情请求分别增加最新请求版本约束；搜索改为受控查询；加载中、书架失败、标签失败和空结果拆成明确状态并提供对应重试。 | store/View 乱序与失败状态测试通过；隔离后端下对搜索、筛选、排序、空态、失败态和快速切书逐项验证。 | FIXED |
| AUDIT-014 | 书架写操作反馈与重复提交 | store 把创建、更新、删除和排序异常吞成 `null`/`false`，上层无法区分真实失败；慢请求期间还能重复提交书籍、标签、章节、批量翻译或排序。 | 写操作保留异常语义并返回真实资源/`void`；各交互 owner 只增加一个在途状态锁，按钮同步 loading/disabled，刷新失败不再反向篡改已成功命令的结果。 | BookModal、TagManageModal、BookDetail 与 store 定向测试覆盖失败和双击；隔离 API 实测创建、编辑、删除及批量操作。 | FIXED |
| AUDIT-015 | 书籍部分更新与前后端契约 | 更新书籍会先读完整详情，再把未修改字段一并回写；后端又把缺失 title 当空串，实际不是部分更新。API mapper 还总是生成 `chapters: undefined`，可能用摘要响应清空已加载章节；创建/更新完成后存在冗余 GET。 | 后端实现真正的字段级更新并严格拒绝非字符串；前端 multipart/JSON 只发送出现的字段，直接消费写响应，mapper 仅在响应确实含章节时写入 `chapters`；同步 OpenAPI 的排序、tagIds、cover 和章节标题结果契约。 | backend stage2 5 tests、API 7 tests 与生成类型一致性检查通过；测试确认摘要更新不会清空详情章节。 | FIXED |
| AUDIT-016 | 标签与选择投影一致性 | 标签加载失败被静默忽略且旧请求可覆盖新标签；重命名/删除完全依赖后续刷新，刷新失败会使列表、当前详情和已选筛选标签互相矛盾；重名更新还会泄漏数据库约束异常。 | 标签加载采用最新请求提交并公开独立错误；成功重命名/删除立即更新当前内存投影，刷新仅作权威校准；重复名称统一返回 409；批量选择在筛选结果变化后剔除不可见项，章节删除同步清理选择。 | store 标签乱序、刷新失败、投影更新与 409 测试通过；书架页和标签弹窗各增加失败重试测试。 | FIXED |
| AUDIT-017 | 书架与详情响应式视觉 | 与 `main` 对照发现无封面占位缩在左上角、搜索框出现原生和自定义两个清除按钮；390px 下详情横向溢出，长标题挤压图片数，章节/标签操作孤立换行，批量工具栏宽度失衡。 | 在对应 owner 内修正占位、搜索原生控件、标题收缩和小屏操作布局；弹窗正文横向约束下沉到 `BaseModal` primitive，未保留业务穿透样式。 | 内置浏览器桌面与 390×844 逐状态复核；当前页面、弹窗、正文及全部后代均无横向溢出；Stylelint、ESLint、UI architecture 通过。 | FIXED |
| AUDIT-018 | 角色工坊前端状态所有权与并发 | 文档/头像隐式 LRU 缓存制造第二事实源；快速切书、切文档和旧请求后返回会污染当前上下文；保存缺少明确 CAS revision，切换动作也可能越过未完成的自动保存。 | 删除前端实体与头像 URL 缓存，后端响应作为唯一事实；以 book/doc/stream 身份约束异步提交，所有写入显式携带 revision，动作前冲刷自动保存，未变化文档不发写请求。 | store、View、Sidebar、Editor 定向竞态/CAS/保存测试通过；真实浏览器快速切换书籍和文档未出现跨上下文状态。 | FIXED |
| AUDIT-019 | 角色候选与创建命令信任边界 | 候选列表硬截断 100 条；候选 payload 可覆盖数据库稳定 ID；浏览器创建角色文档时回传整个 candidate/kind/document，使服务端信任可伪造派生数据。 | 派生层提供无任意上限的候选读取与按 ID 查询，稳定 ID 最后覆盖 payload；创建命令仅接收 `candidateId`/`title`，后端在当前 book 中解析有效候选并生成文档。 | 205 个候选、稳定 ID、跨书/伪造 candidate 拒绝和前端最小命令测试通过。 | FIXED |
| AUDIT-020 | 角色工坊组件生命周期、可访问性与数据流 | 资源弹层自建遮罩、世界书递归深拷贝 watcher、会话工具栏伪装 ARIA menu、部分操作忙碌态不一致；与 `main` 对照时还需防止恢复其失效的移动端布局。 | 资源弹层归并 `BaseModal`；世界书改为不可变事件；会话工具栏改为真实 disclosure region 并补齐焦点/Escape；统一 action disabled，清除候选旧字段别名，并保持当前窄屏工作区布局。 | 组件测试、键盘生命周期测试通过；内置浏览器覆盖全部编辑/预览标签、弹层、菜单、空态及 390×844 布局，无横向溢出；`main` 同宽布局已确认失效。 | FIXED |
| AUDIT-021 | 角色工坊输入、导入与模型输出契约 | 路由用宽松 `int`/字符串转换接受 bool、对象等非法标量；会话导入容许错误消息、重复 ID、半套摘要和非对象 runtime log；摘要/审阅/生成 section/聊天回复又会把畸形服务商结果转换成看似成功的数据。 | 请求标量、消息/资产 ID、summary pair、runtime state/log 全部严格验证；摘要统一为 `{summary: string}`；审阅和各生成 section 只接收当前 canonical 结构；空聊天或错误类型立即让 operation 失败，不伪装成功。 | Backend Studio 35 tests 覆盖非法标量、重复 ID、错误摘要/section/聊天；前端 API/store 契约测试同步通过。 | FIXED |
| AUDIT-022 | 角色工坊聊天与 Agent 提交竞态 | 发送失败时会过早清空草稿/保留瞬态消息；Agent 失败可能丢失待采纳结果；极快 operation 在下次渲染前完成时，依赖 `abortable` 状态边沿无法判断提交是否真正被后端接收。 | 草稿仅在 `onAccepted` 后清空，失败回滚瞬态消息并保留输入；Agent 输出只在成功采纳后清理；引入单调 accepted submission 计数，独立于 operation 完成速度。 | Preview/store 定向测试覆盖接受前失败、操作回滚与“同一渲染帧内完成”竞态；全部角色工坊前端测试通过。 | FIXED |
| AUDIT-023 | 角色工坊诊断与错误状态 | 诊断只检查世界书顶层节点，嵌套条目漏报；前端只按 `valid` 显示“通过/未通过”，有警告或错误时无法准确表达状态。 | 诊断递归展开世界书条目；状态按错误、警告、valid 的明确优先级显示“存在错误/存在警告/通过/未通过/未执行”。 | nested lorebook 后端测试和 Sidebar warning/error precedence 测试通过；真实 API 文档显示“存在警告”且计数一致。 | FIXED |
| AUDIT-024 | 角色文档更新 SQLite 事务 | 更新文档先提交一次 `BEGIN IMMEDIATE`，随后为了同步活动草稿又开启第二个写事务，放大锁窗口，并让 replay 路径与首次执行路径不一致。 | 在同一短写事务内完成文档更新和活动草稿对齐，删除第二次 `BEGIN IMMEDIATE`；新写入的 runtime log 统一存储 canonical 空数组。 | 草稿问候语对齐与幂等回放回归测试通过，Studio 后端测试全绿。 | FIXED |
| AUDIT-025 | 角色工坊 OpenAPI 与前端映射边界 | Studio 顶层响应允许任意额外字段，消息缺少三个必需运行态字段；前端 mapper 对未知 role 和畸形消息继续做默认 assistant/字符串兼容，掩盖契约漂移。 | Studio 顶层 schema 改为 `additionalProperties: false`，消息显式要求 runtimeLog/variablesSnapshot/generationMeta；mapper 直接消费生成类型，不再猜测 role 或兼容任意对象。 | OpenAPI 客户端连续两次生成 SHA-256 一致；typecheck、API 测试与生成文件差异检查通过。 | FIXED |
| AUDIT-026 | Insight 提示词设置持久化边界 | 设置弹窗中的提示词保存、恢复默认和导入曾分别走前端局部状态与多次写请求，设置和提示词无法形成同一提交边界；导入还会接受缺字段/旧形状并以默认值补齐，继续维持旧数据兼容。 | 设置事务加入严格的提示词 mutation，后端在同一事务内校验并提交设置与提示词；恢复默认由后端返回权威工厂提示词；导入只接受当前 v2 完整结构，缺失四类工厂提示词或额外/错误字段直接拒绝，不做迁移。 | Backend settings 46 tests、前端 API/设置测试通过；OpenAPI 确定性生成通过。 | FIXED |
| AUDIT-027 | Insight 设置异步请求与诊断所有权 | 模型列表、连接测试和设置加载可被重复触发，旧请求后返回会覆盖较新的 provider/config；模态框关闭或切换上下文后仍可能提交旧响应，多个标签又各自复制相同的连接测试状态机。 | 模态框、模型发现和连接测试均使用单调请求身份与当前 provider/config 快照约束提交；四个模型标签共享一个有真实消费者的连接测试组合函数，统一忙碌态和错误传播，删除重复的错误格式化扩展点。 | 连接/模型乱序与重复提交测试通过；设置相关 10 个测试文件共 110 tests、typecheck 和 ESLint 通过。 | FIXED |
| AUDIT-028 | Insight 设置当前契约与批量分层 | Batch 标签保留 `units_per_group`/`align_to_chapter` 旧字段回退并复制 draft watcher；配置 hydration 用 `value || 1` 把当前合法的 `0（汇总全部）` 改成 `1`；应用配置 mapper 又对必需字段做字符串/数字/布尔强制转换，掩盖后端契约漂移。 | 删除所有 Batch 旧字段兼容和重复 watcher，只消费 canonical `units`/`align`；严格验证当前应用配置的完整精确结构，不在浏览器补默认值；用直接赋值保留末层 `units=0`，同时移除无消费者的 provider wrapper。 | 零单位回归测试与畸形 payload 拒绝测试通过；内置浏览器实测自定义模式末层仍为 `0（汇总全部）`；Stylelint、ESLint、typecheck 和 110 tests 全绿。 | FIXED |
| AUDIT-029 | 续写前端事实与分页所有权 | API facade 用模块级 LRU 缓存项目、角色形态及表单，写命令会基于浏览器旧 revision；分页 cursor/has-more 又同时存在于 API 与两个界面 owner，切书时旧响应可合并进新书。加载原作参考图还被迫读取无关的续写项目和全部形态。 | 删除全部前端续写实体缓存，每条写命令先读取当前后端项目；cursor 只由对应 composable/面板持有，并以 book/request 身份约束提交；拆出只读取原作缩略图的入口，删除重复布尔和无消费者计数。 | 乱序、切书、分页合并及“每次 mutation 读取当前 revision”测试通过；续写定向 12 files / 121 tests、typecheck 与 ESLint 通过。 | FIXED |
| AUDIT-030 | 续写当前数据与命令契约 | 路由曾用 `int`/`bool`/默认值接收缺失或错误类型；不同 job kind 会静默忽略无关字段，空 ordinals 被当作全部；项目、页面和冻结分析快照还存在默认值/旧 run fallback。模型返回错误类型也可能被 `str`/`list` 转成成功页面。 | 请求、项目配置、页面 payload、冻结分析输入和 provider 结果全部按当前精确结构严格验证；job 命令改为 OpenAPI discriminated `oneOf`，拒绝空/重复/布尔页码及无关字段；损坏或旧项目要求清除，不迁移、不回退；页面发布冲突和畸形模型结果直接使步骤失败。 | Backend Insight 44 tests、OpenAPI 9 tests、compileall 和确定性 API 生成通过；覆盖缺字段、非法类型、旧结构及畸形 provider 结果。 | FIXED |
| AUDIT-031 | 续写数量边界、三视图与版本历史 | 前端/契约对续写页数和参考图设 100/20 硬上限；角色三视图声称支持 1–5 张源图但后端只消费第一张；提示词预览截断正文；图片版本 DTO 和 Worker 清理又只保留最近 5 个，永久丢失历史。 | 删除无依据的页数、参考图和引用资产硬上限；三视图明确只接收一张源图；提示词完整保留并通过折叠控制展示；返回并保存完整图片版本历史，不再后台删除第 6 个以后版本；移除死字段 `characterForms`。 | 250 页/50 参考图、6 个页面及形态版本历史测试通过；内置浏览器确认三个数值控件均无 `max`，四步、参考图与三视图弹层无横向溢出。 | FIXED |
| AUDIT-032 | 续写页面剧情与图片状态投影 | 后端 `ContinuationPagePayload.status=ready` 表示页面剧情已就绪，前端却仅在存在图片版本时映射为 `generated`，导致已生成剧情在第三步显示“待处理”；同一个状态又被图片卡片误当作图片状态。 | `ready` 始终投影为剧情“已生成”；图片卡片仅按图片版本是否存在（以及剧情 stale）计算“待生成/已生成/需重新生成”，删除从未被真实任务驱动的逐卡 loading 样式，生成按钮统一服从当前批次忙碌态。 | 新增“ready 剧情但无图片”API/组件回归测试；121 tests 通过；内置浏览器实测第三步三页均“已生成”、第四步同三页均“待生成”。 | FIXED |
| AUDIT-033 | Insight 当前配置与派生结果契约 | Insight 配置解析仍会把字符串/数字/布尔值强制转换为当前字段，自定义层级又存在 8 层、每组 100 单元、批次 20 页和上下文 10 批的任意上限；派生向量、时间线与当前分析结果还会接受缺字段或旧形状，掩盖存储和模型契约漂移。 | 当前配置改为精确字段和精确类型，删除无产品依据的层级/批次/单元数量上限；派生数据只消费当前 schema，旧数据和畸形服务商结果直接失败，不迁移、不补默认值。 | Backend Insight 60 tests、前端配置/API/store 定向 30 tests 通过；覆盖 9 层、21 页、11 上下文批次、101 单元及畸形 payload 拒绝。 | FIXED |
| AUDIT-034 | 图片/容器/网页导入与设置聚合数量边界 | 导入层对 ZIP 条目、PDF/MOBI/容器页数、网页候选、HTML 字节数、页面文档 mutation、翻译约束条目和设置 JSON 总字节数设置了与机器资源和业务语义无关的硬上限；大批量合法输入会被拒绝，HTML 又必须先完整驻留内存。 | 删除上述任意数量/聚合字节门槛；保留压缩比和流式块大小等真实安全不变量；网页 HTML 改为增量解码并逐块送入解析器，页面/约束/设置按当前结构验证而不按任意总量截断。 | Platform、Content、Web Import 组合 110 tests 通过；覆盖超过 10000 个网页候选、10001 条术语、501 个气泡 mutation 和大型章节设置。 | FIXED |
| AUDIT-035 | Content HTTP、嵌入文档与快速工作区契约 | Content 路由会用 `int`/布尔真值/字符串转换接收错误标量，部分命令允许缺字段或无关字段；页面样式、章节设置记忆和翻译约束会读取或覆盖旧 schema，颜色/约束重复项又存在静默修正；仅含样式传播的页面 mutation 被误判为空操作。快速工作区提升还会原样复制旧约束，标签批处理实现与 OpenAPI 的唯一性声明不一致。 | 所有请求按当前精确类型和 discriminated command 校验；嵌入文档只接受当前 schema，旧数据直接拒绝；颜色、约束和气泡 payload 严格验证；修复传播-only mutation；快速提升只复制验证后的当前约束，并严格拒绝旧目标/旧源和重复标签 ID。 | Content 60 tests、compileall、`git diff --check` 通过；覆盖非法标量无副作用、旧 schema 保持未改写、10001 条约束、501 个气泡、传播-only 更新及快速提升事务回滚。 | FIXED |
| AUDIT-036 | Job 队列状态图、重试与并行 Worker | 任务详情按 item 逐次读取 steps 形成 N+1；空 `allowed_kinds` 会被当作全部步骤；直接/排空取消和任务失败只改 job 终态而遗留 pending/running 子图；checkpoint/event 不能按冻结 credential 真值脱敏；路由、重试快照和 Worker 配置大量使用字符串/整数/布尔强转及旧字段默认。锁等待进度写入还可能因 SQLite BUSY 反向失败真实图片步骤。 | 详情改为固定查询聚合；空 kind 不领取；取消/失败在同一事务闭合全部 item/step；所有持久事件按实际冻结 secret 脱敏；重试只接受当前 schema 的完整精确结构；路由和 Worker 拒绝错误类型；锁等待进度改为有限重试的非权威遥测，持续锁竞争只略过该次遥测。 | Stage3 Jobs 43 tests passed，覆盖 25 items 仍固定 5 条 SQL、取消/失败子图、secret canary、空 kind、旧 schema/畸形快照、错误并行配置和持续 SQLite BUSY；compileall 与 diff check 通过。 | FIXED |
| AUDIT-037 | Operation、repair、render attempt 与页面并发 | attempt 心跳失租后没有同 epoch 恢复路径，operation/render 可永久停在 running；远程 operation 若一律重做会重复 Provider 副作用；旧 revision 渲染失败会把已合并的新请求标成 failed。operation active 期间页面仍可编辑；已有 render chain 时后续非文字字段编辑不推进 requested revision，导致旧渲染反复淘汰。repair 又硬编码样式 fallback、允许空几何/任意颜色、资源打开中途失败时泄漏，并对请求标量和事件 secret 做宽松转换。 | 当前 epoch 的持权调度器在租约过期后只重做本地确定性 operation/render，远程 operation 明确失败且不重放；完整校验 epoch/attempt/lease 后隔离旧渲染失败并保留最新 revision；页面 mutation 原子拒绝 active operation，存在 pending/running render 时推进最新 revision；repair 从当前页样式继承并严格验证 geometry/#RRGGBB/二值 PNG，统一关闭图片资源；HTTP/event/idempotency 与 secret redaction 全部收紧。 | Operation 27 tests、Content 60 tests、OpenAPI 9 tests 及组合 87 tests 通过；覆盖过期本地/远程 attempt、旧 fence、旧失败与新请求、非渲染编辑推进、active operation 编辑拒绝、样式继承、空 mask/坐标和 secret canary。 | FIXED |
| AUDIT-038 | Worker/API epoch 失联恢复闭包 | epoch 状态曾只检查 `active` 而忽略已过期租约；Worker 失联只改 job 顶层状态，遗留 running item/step；API/Worker 恢复重排 operation 没有 durable event；API render 恢复后页面仍停在 `rendering`。 | `is_active_epoch` 同时校验租约；同一恢复事务按 interrupted/cancelling 闭合 item/step/lock；为失败或重排 operation 写入权威事件；render 重排同步把仍在渲染的页面标为 stale。 | Stage1 Platform 48 tests、Stage3 共享轮询恢复测试及 Windows 角色边界 14 tests 通过；覆盖失联恢复幂等、任务子图、operation 事件和页面状态。 | FIXED |
| AUDIT-039 | Worker 模型释放与 SQLite BUSY | 手动模型释放已经执行后，若完成命令的 SQLite 写入遇锁，异常会被误记为模型卸载失败；下一轮又不能领取本 epoch 留下的 running 命令。默认 PaddleOCR ONNX 单例也没有释放入口，违反“释放全部本地模型”的当前契约。 | 同一 Worker epoch 可幂等续办 running 释放命令；只有实际 resetter 失败才标记命令失败，完成持久化的 BUSY 交给调度安全点有限重试；补充 RapidOCR/ONNX session resetter。 | Worker model lifecycle/maintenance 8 tests 与 Runtime heartbeat 4 tests 通过；覆盖首次完成写入 BUSY、第二次续办完成及 Paddle ONNX session 释放。 | FIXED |
| AUDIT-040 | Insight 动态层 Worker 处理器 | 设置和任务图已经允许超过 8 层，但 Worker 入口仍只注册 `insight_build_layer_0` 至 `_7`；第 9 层任务可成功持久化却会在执行时以 unsupported step 失败。 | Worker 在领取冻结 attempt 后按实际 step kind 解析 canonical 非负层索引，不设固定上限；删除 0–7 的硬编码注册循环，同时保持未知 kind 明确失败。 | Stage3 动态 step 测试、角色边界 resolver 测试和 Worker 完整注册核对通过；验证 0、8、128 可解析，`08` 与非法后缀拒绝。 | FIXED |
| AUDIT-041 | 过期 Worker 共享轮询测试假阳性 | `is_active_epoch` 加入租约判断后，原测试仅等待该函数为 false；测试线程尚未执行恢复时租约本身已使条件为 false，无法再证明无浏览器情况下的权威恢复。 | 测试改为直接轮询 `process_epochs.status`，必须观察到后台轮询器将状态从 active 改为 lost，再核对 epoch 不可用。 | 修正后的定向测试与 Stage3 Jobs 相关测试通过。 | FIXED |
| AUDIT-042 | 当前气泡文档与前后端持久化边界 | 重构后的前端适配器、工厂、OpenAPI 与后端 `BubbleState` 曾同时容许部分字段、默认补值或宽松类型；页面 repair 推进 document revision 时又未同步气泡 revision，下一次严格编辑会把刚修复的当前数据判断为过期。 | 将气泡收敛为唯一完整 23 字段当前 schema，前端只创建/提交完整文档，后端拒绝部分或旧字段；页面 repair 在同一事务推进页面及全部气泡 revision，不添加迁移或兼容读取。 | Stage3 Operations 29 tests、Stage4/算法组合测试通过；OpenAPI 客户端重新生成并核对哈希。 | FIXED |
| AUDIT-043 | 检测配置、几何与模型失败语义 | Worker 检测适配器会静默过滤未知字段并用核心默认值补缺字段；部分检测后端对畸形几何、结果基数和内存/原生模型异常处理不一致，可能把错误降格为空检测结果。 | 任务准入和 Worker 算法适配器都要求当前完整检测配置；所有检测器、后处理、refine 与大图合并严格校验类型/尺寸/基数，模型及分配失败直接使步骤失败。 | 检测树逐文件/调用链审查完成；Stage4、内存错误与模型失败定向组合 133 tests passed。 | FIXED |
| AUDIT-044 | OCR/颜色执行链与 AI 视觉冻结配置 | OCR 入口仍保留空值/“无”语言兼容、提示模式归一化和未知语言默认日语；AI 视觉冻结的 OpenAI 选项字典没有物化为执行层类型，`paddleocr_vl` 当前提示模式还被接口误拒绝。部分本地 OCR 初始化可竞态，错误结果/分配失败边界不统一。 | 删除旧语言与提示模式 fallback；严格校验图像、坐标、文本行、引擎与结果基数；恢复当前 `en`/`paddleocr_vl` 协议；Worker 将冻结选项显式物化；本地模型只在成功初始化后发布线程安全单例，服务商或内存错误均向任务失败传播。 | OCR/Provider/Stage4/内存错误组合 133 tests passed；新增当前提示模式、英文代码、越界坐标、精确配置及选项物化回归。 | FIXED |
| AUDIT-045 | LaMA 修复失败、资源与 revision 一致性 | 选择 LaMA 后异常或空结果会回退纯色并标记成功；接口还会跨模型切换、逐页强制 GC/CUDA 清理，MPE 会扭曲尺寸且未严格校验 checkpoint/tensor。修复算法额外返回无人使用的背景副本，operation 插件可替换为错误尺寸资源。 | 所选 LaMA 模型必须成功或使步骤失败；MPE 仅填充到 8 倍数后裁剪，严格检查权重、张量形状/有限值和尺寸；删除跨模型/纯色 fallback、逐页清理、冗余副本与任意侵蚀启发式；repair 插件资源必须是同尺寸 PNG，并同步页面/气泡 revision。 | LaMA/修复定向 10 tests、Stage3 29 tests、Stage3+Stage4+算法组合 106/133 tests passed；实际 bundled checkpoint 加载成功。 | FIXED |
| AUDIT-046 | 权威文字渲染、字体与自动字号 | 自动字号二分循环会吞掉字体加载/度量异常并返回最小字号；字体入口把字符串、小数和布尔值隐式转成字号，统一渲染又对无效字号/坐标使用默认或跳过。旋转气泡允许当前 schema 的有限小数位置偏移，却把小数直接传给 Pillow `paste` 而失败。 | 字号只接受精确正整数；自动字号、字体和任一气泡渲染错误全部向 operation/job 传播；删除无效字号、坐标和最小气泡宽高 fallback；旋转最终粘贴点按像素四舍五入；保留当前字形回退与视觉度量降级，但内存/原生分配失败始终上抛。 | 逐行核对当前渲染/字体/服务/路由及 `main` 旧编排差异；渲染+Stage4 组合 91 tests、Content/样式组合 76 tests passed。 | FIXED |
| AUDIT-047 | 翻译服务商、共享 AI 传输与重试边界 | 翻译适配器存在 HTTP 明文/全局可变凭据与嵌套重试风险；共享 OpenAI 选项会补齐旧/缺字段并强转类型，运行时保留第二套 `request_overrides` 可覆盖统一请求；流响应会跳过畸形 JSON、仅识别 `data: `，同步 keepalive 可无限延长，嵌入/模型列表又接受缺索引、重复或启发式过滤。普通翻译还按任意 4000 字拆分一页，并靠错误消息关键词判断是否重试。部分测试在导入时用残缺 `yaml/openai` 模块污染整个测试进程。 | 百度/有道/彩云改为官方 HTTPS 单请求与逐调用凭据；共享配置只接受当前完整精确结构，删除 `request_overrides` 和所有隐式降级；同步/异步响应、SSE、JSON、嵌入、模型列表及能力/凭据全部严格校验，网络重试后有内容不再重放，内存错误立即失败；合法 SSE 可无空格，keepalive 受总截止时间约束。整页翻译不再按字符硬拆，适配器只对连接/超时/429/5xx/明确空结果有限重试，认证/配置/协议错误立即失败；删除测试全局兼容桩。 | 翻译失败语义 32 tests、共享 Provider/Transport/Insight 组合 96 tests、Stage4+翻译 99 tests、设置/Web Import/Stage4/Insight/Plugins 上层组合 204 tests passed；py_compile 与定向 diff check 通过。 | FIXED |
| AUDIT-048 | Insight Provider 配置与保留客户端边界 | 漫画分析仍保留旧宽松序列化器、通用 BaseClient/JSON 猜测解析器、未被当前调用的 VLM 批量/上下文/default prompt/连接测试接口，以及 Embedding/Chat 的 raw generate、临时温度覆盖和 no-op 生命周期；Provider 配置还会靠默认值或标量转换接收缺字段。真实 Worker 每个步骤只分析一页，这些双轨表面扩大了状态和错误边界，并可能把畸形服务商结果包装成当前结果。 | 删除旧配置包、通用基类与猜测解析器，不提供导入兼容；冻结配置和凭据快照只构造当前严格 dataclass；VLM 收敛为单页、显式 prompt 和精确页码/JSON 协议；Embedding/Chat 只保留 QA/派生结果的真实入口，ImageGen 保留其实际拥有的 HTTP 生命周期；所有模型、内存和协议错误直接传播。前端 provider hydrate/payload 同步只接受和发送当前完整结构，并删除身份包装及隐藏 RPM fallback。 | Manga Insight 客户端/内存定向 33 tests、Stage5 61 tests、共享上层组合 200 tests、前端配置 135 tests 与 typecheck 通过。 | FIXED |
| AUDIT-049 | Insight 分析命令、冻结配置与 run 终态 | 分析重放会在缺少当前 `runId` 时回查旧 run；命令、冻结 target 和配置存在字符串/整数/布尔强转，已无语义的 `force` 仍散落于路由、resolver、重试及内部调用。Worker 验证失败在 `complete_step` publisher 内先写 run failed 再抛错，整个事务回滚，导致步骤失败但 run 仍停在 staging。前端又把 OpenAPI 必填 `runId` 做成可选交叉类型，并保留重复 QA 消息/模式声明和无消费者 `AnalysisMode`。 | 重放必须携带当前精确 `runId`；分析命令、selector、冻结 target/layer 全部精确校验；彻底删除 `force`，resolver 改为显式 `scope`；验证失败通过 `fail_step` 的失败事务原子持久化 run 终态。前端分析创建使用 `InsightAnalysisJobAccepted`，`runId` 必填，QA 类型集中到唯一领域声明并删除死类型。没有旧数据回查、迁移或兼容分支。 | Backend Stage5 67 tests、前端 Insight/任务中心/QA 26 tests、TypeScript typecheck 与受影响 Python py_compile 通过。 | FIXED |
| AUDIT-050 | Insight QA 临时请求、模型结果与前端流契约 | 过期的 running 检索 attempt 不能被重新领取，断开的 pending/running 连接也不会被清除，会永久占用每本书唯一活动请求；API 连接没有心跳。检索候选、全局上下文、重排结果、数据库状态和 SSE 前端解析又存在额外字段、部分结果、默认空对象及 `String`/`Number` 强转，模型空流仍会发送 `done`。前端切书只删除 loading 消息，上一部书的完整问答会残留。问题长度、Top K 和内部候选/关键词数量还存在无产品依据的硬上限。 | API 与 Worker 共同续租同一临时请求；过期 attempt 可 fenced 重领，失活请求由维护任务删除，不增加迁移表或兼容分支。当前请求、候选、父级上下文、全局必需 artifact、重排基数、数据库字段和 SSE 事件按精确结构验证；服务商空回答直接失败。删除任意问题/Top K/候选及关键词截断；切书原子中止旧流并清空单轮记录；QA 控件直接消费共享组件的当前类型。 | Backend Stage5 72 tests、前端 QA/API 32 tests、typecheck、ESLint、compileall、定向 diff check 全部通过。 | FIXED |
| AUDIT-051 | Insight 派生结果、时间线 generation 与当前快照边界 | 派生层、总览和压缩上下文会把空或部分 Provider 结果发布为成功；页面局部重分析后仍可能复用旧全量 run 的层级和压缩上下文。时间线又允许模型自带持久 ID、缺失或不一致页引用、宽松 metadata/角色内容，读取端用类型强转和默认归一化掩盖损坏数据，分页还可能把两个 generation 合并。向量构建会跳过缺失页面摘要并吞掉部分存储清理错误。 | 快照只在全部当前页面同属活动 run 时暴露 source run；派生输入、Provider 输出、artifact、timeline、vector checkpoint/publication 和数据库字段均按当前精确结构验证。时间线服务端生成稳定 ID、canonicalize 成对页引用并校验 metadata；前端严格解析完整响应，分页 generation 改变时整体替换。缺失摘要、空结果、损坏旧数据和存储错误直接失败并提示清除，不迁移、不补默认值。 | Backend Stage5 85 tests、前端时间线/API 28 tests、typecheck、ESLint、Prettier、py_compile、OpenAPI 确定性生成和 diff check 全部通过。 | FIXED |
| AUDIT-052 | Insight 命令/导出准入、页面发布状态与续写图片发布 | 分析目标和导出快照在读取后、任务真正入队前可能被并发改动，导致任务冻结已经失效的页序、分析头或派生资产；页面 target 已完成但完整 run 尚未发布时会提前回落为旧的 ready 状态。续写图片还会在确认页面输入仍有效前调用模型，浪费服务商请求；派生层及 Worker 保留的生产 `assert` 在 Python 优化模式下会消失。 | 在同一个短写事务内重新解析并核对分析目标、页源、顺序及导出 run/page/artifact/timeline 快照，冲突时整笔拒绝且不留下任务；幂等重放先于可变状态读取。页面状态在对应任务终态前保持 running，run 预览严格验证书籍、页面、target 与结果完整性。续写图片调用模型前增加 revision 预检并在发布时再次 CAS；所有相关生产断言改为显式领域冲突。未加入迁移、兼容读取或数量限制。 | Backend Stage5 99 tests passed；新增分析/导出准入竞态、非 canonical cursor、run 预览、发布窗口、失败 run 幂等及续写 preflight 回归。 | FIXED |
| AUDIT-053 | Insight 前端页身份与时间线缩略图事实源 | API facade 用模块级 Map 缓存页码到页面 ID 和时间线缩略图，并任意限制为 300 条；同一本书重排、重分析或 timeline generation 更新后可继续命中旧页面。参考图选择器又忽略自身已获得的资源路径，反向依赖这份缓存；时间线加载更多还用角色名称和统计数猜测 cursor。 | 删除全部模块缓存和 300 条上限；页码命令每次通过后端解析稳定 ID，缺页整笔失败；时间线响应自含并严格验证 `page_thumbnails`，分页只使用后端 cursor、稳定事件/角色 ID 和 generation；参考图直接使用当前后端资源路径。 | API 21 tests、时间线 12 tests、参考图 11 tests、typecheck、ESLint 通过；覆盖页面重排后重新解析、缺页不提交及当前缩略图投影。 | FIXED |
| AUDIT-054 | Insight 页面树与详情状态投影 | 页面树把除 `not_analyzed` 外的 running/failed 都标成已分析，章节也错误显示完成；刷新只监听已分析总数，重分析前后计数相同就保留旧缩略图状态。详情 facade 又把五态压成 boolean，并靠摘要存在推断完成，使运行中、失败和过期都显示成错误状态；刷新 key 使用毫秒时间戳还可能在同一毫秒丢事件。 | 只有 ready/stale 视为已发布结果；页面树同时监听权威刷新序列并 fenced 重载已展开列表。详情完整保留并分别呈现 not_analyzed/running/failed/stale/ready，空摘要文案和操作状态随真实状态变化；刷新 key 改为单调递增；删除失效样式并传播真实导出错误。 | 页面树 14 tests、页面详情 25 tests、API 21 tests、typecheck、ESLint、Stylelint 通过；覆盖同计数终态刷新与五态显示。 | FIXED |
| AUDIT-055 | Insight 笔记 DTO、筛选分页与引用更新 | API 与 store 各定义一份不同的 NoteData，二次宽松 mapper 把当前必填 revision/title/tags/citations/timestamps 当可选；类型筛选只过滤已加载的前 50 条，后页匹配项会被误报为空。QA 回答同时镜像在 answer/content；手动笔记修改关联页码没有更新 citations，而 QA 标题编辑又可能把多页引用压成首个 pageNum。 | NoteData 收敛为唯一当前完整 DTO，API 边界严格校验并删除重复 mapper 文件和 answer 镜像；筛选与后续分页都把 kind 交给后端且 fenced 旧筛选响应；手动 pageNum 更新显式解析稳定页面 ID，缺页失败，QA 编辑仅修改可编辑标题而完整保留引用。问题预览不再截断数据，只用 CSS 控制视觉行数。 | 笔记组合 45 tests、API 21 tests、typecheck、ESLint、Stylelint 通过；覆盖跨筛选竞态、分页去重、malformed DTO、页码更新及缺失引用拒绝。 | FIXED |
| AUDIT-056 | Insight 概览内容与最近分析错误状态 | 面板先依赖“已生成模板列表”再决定是否读取概览，元数据请求失败或短暂不一致时会把真实内容伪装成空；最近分析失败也被清空成“暂无记录”。本地还保留未显示的 analyzed_at、死 CSS、固定 150px 摘要宽度和窄栏不换行头部。 | 当前模板始终直接读取权威概览；模板列表只作为辅助状态且失败不遮蔽内容。最近分析失败显示真实错误和重试操作；删除无消费者字段/样式/固定宽度，并让头部与操作区在窄栏换行。 | 概览 16 tests、API 21 tests、typecheck、ESLint、Stylelint 通过；覆盖元数据失败仍显示内容、最近分析错误不伪装空态及跨书请求 fencing。 | FIXED |
| AUDIT-057 | Insight 分析进度新旧任务投影 | 重新分析受理后只替换任务 ID 和状态，仍沿用上一任务的进度，导致新任务排队阶段可能显示旧任务 100%；状态联合与组件还保留后端快照、任务中心和任何生产调用都不会产生的 `error` 分支。页码输入同时在父容器和字段自身重复添加上边距。 | 新任务受理时立即清空上一任务进度，等待任务流投影权威总量；删除无来源的 `error` 状态及其自证属性数据；移除重复页码间距，不增加旧状态兼容。 | 分析进度单元/属性共 21 tests、typecheck、ESLint、Stylelint 通过；新增旧进度不会泄漏到新任务的回归测试。 | FIXED |
| AUDIT-058 | Insight 时间线当前版本、状态与呈现契约 | 初次加载失败会同时显示错误和“尚未生成”；重新生成受理后主动清空仍可读的旧版本，完成前只有空白提示，且排队后可重复提交。增强模式通过摘要/旧字段猜测；剧情弧允许缺失或重复 ID，角色按名称做 key。摘要卡还把伏笔误标为“主题”并任意截前 5 条；事件卡展开后重复同一摘要，剧情弧展开没有稳定的新增信息。后端仅验证事件/角色，畸形剧情弧或伏笔可发布后才在前端失败。 | 加载错误与真实空态分离并提供重试；生成期间保留旧版本、显示排队反馈并阻止重复提交，版本切换清空旧展开状态。模式只信任权威 `mode`，剧情弧/角色使用稳定 ID，统计只接收单一 stats，删除伏笔主题镜像和数量截断；摘要展开改为解除行数折叠而不是重复正文。前后端同时要求剧情弧稳定 ID/描述并拒绝重复身份和畸形伏笔，OpenAPI 补齐当前结构，不做旧数据兼容。 | Stage5 100 tests、OpenAPI 9 tests、时间线/API 48 tests、typecheck、ESLint、Stylelint、py_compile、确定性 API 生成与 diff check 通过；全部时间线视觉组件保留 REVIEWING 等待页面实机验收。 | FIXED |
| AUDIT-059 | Insight QA 呈现、任务恢复、SSE 与笔记保存 | 向量重建确认错误宣称会先删除当前索引，确认期间卸载仍会提交；中断任务和已受理的概览/压缩上下文修复无法从任务中心恢复投影，可重复排队。切换问答模式不会中止旧流，旧答案可落入新模式；全局请求仍发送无关精确检索参数，SSE 允许正文早于上下文且 done 重复引用/建议字段。消息保留死时间戳/来源字段，保存笔记又任意截断默认标题；视觉 mock 使用错误协议顺序和旧字段。 | 任务中心按精确 target 投影向量及派生任务，中断状态可恢复，确认/请求均做组件、书籍和模式 fencing；生成新索引期间保留当前版本并阻止重复提交。QA 请求改为判别联合，全局模式不发送无关参数，严格要求 status→context→chunk→done 且 done 为空；删除建议问题及消息镜像字段。笔记保留完整问题标题、清理空白并阻止重复保存；修正视觉 fixture 为当前协议。 | 前端 7 文件 58 tests、Backend Stage5 100 tests、typecheck、ESLint、Stylelint、py_compile 和定向 diff check 通过；QA 视觉组件保留 REVIEWING 等待 Insight 页面实机验收。 | FIXED |
| AUDIT-060 | Insight 入口装载、路由、书籍/章节选择与任务事件投影 | 入口先发布半加载书籍并挂载全部子面板，核心请求失败后仍可能展示伪造章节/空状态；书籍选择器复用书架页筛选结果，直接路由又先加载整个书架，造成空列表、重复请求与竞态。移除 book 查询不清理投影；任务完成事件早于任务中心快照时会永久丢失，中断任务只查队列不查历史。标签切换销毁面板、ARIA 控件与面板不闭合，键盘只发事件不移动焦点，未选书时还预留空右栏。 | 书籍详情、分析状态和章节改为原子核心快照，任一失败都显示真实错误且不挂载子面板；删除章节伪回退和状态伪造。选择器直接读取无过滤书籍列表并 fenced 加载/卸载；路由 book 即时装载，清空时原子清理书籍状态与瞬时界面。任务投影同时观察队列/历史并等待真实终态，标签面板持久化且补齐 tab/tabpanel 关联和实际焦点，右栏由显式状态控制；章节确认重新校验当前身份。视觉夹具补齐当前概览必填 title，不加入兼容、迁移或旧数据 fallback。 | 7 个定向文件 96 tests、typecheck、ESLint、Stylelint、diff check 全部通过；Playwright Insight 桌面/移动/弹窗 8 tests 通过；内置浏览器完成当前与 `main` 空态对照。 | FIXED |
| AUDIT-061 | 任务中心与 Insight 进度投影 | 后端和 OpenAPI 已提供完整、非负的 JobProgress/Pool DTO，前端仍对计数和布尔字段做 `Number`/`Boolean` 强转并把异常值静默归零；总进度及 Insight 页面进度又夹断超量事实，缺页面池时还用动态 target.pageCount 重建进度，可能把发布步骤误算成页面。并行池丢弃 total/failed/skipped，只显示“完成”，无法看出部分失败。部分测试继续构造缺必填字段和零 ordinal 的旧式 DTO。 | 投影层直接消费当前 typed DTO，不再重建、补默认或遮蔽不一致；只有真实 Insight 页面池才使用页面进度，展示百分比仍限于 CSS 宽度。并行池完整保留并显示 total/completed/failed/skipped/processing/waiting；动态目标页数只接受精确安全整数。测试夹具全部改为当前完整 JobProgress/Pool/CurrentStep 结构。 | 进度/任务中心 18 tests、相邻任务中心/Insight 组合 84 tests、typecheck、ESLint、Stylelint 与 diff check 通过；TaskCenterDrawer 保留 REVIEWING 等待真实任务卡片视觉验收。 | FIXED |
| AUDIT-062 | Insight Store、笔记写入与设置草稿 | 笔记组合层把创建/更新/删除异常吞成 `null`/`false`，Store 再改写为笼统错误，问答笔记弹层因此无法展示后端真实失败原因；Store 同时保留完全等价的 `selectPage`/`setCurrentPage`。设置模态只浅拷贝嵌套 OpenAI `extraBody`，服务商 manager 的 `switch` 依赖动态 `this`，生图标签又把完整 DTO 中的明确空值当成缺失值回填；提示词更新采用合并，无法真正删除已移除键。一个源码测试还继续断言已删除的旧序列化辅助函数名。 | 笔记写操作保留错误状态并原样抛出，两个笔记 UI owner 显示真实异常；页面选择收敛到 `selectPage`。设置草稿统一深拷贝，manager 改为词法函数调用，仅在服务商从未缓存时建立当前 manifest 默认草稿，缓存中的明确空值原样恢复；提示词按完整当前草稿替换。同步更新当前共享序列化源码契约测试，不加入浏览器旧配置读取、迁移或兼容分支。 | 12 个相关文件 121 tests、typecheck、ESLint、Stylelint 与定向 diff check 通过。 | FIXED |
| AUDIT-063 | Insight 设置 DTO、事务完成语义与重复映射 | 后端当前 camelCase 设置先被 API facade 转成一份手写 snake_case `AnalysisConfig`，Store 再通过三份 hydration/payload helper 转回 camelCase，保存时完整反向走一遍；同一配置因此拥有三种形状和两轮手写校验。设置事务成功后 API 内部和设置模态还会连续重新读取设置与提示词，后续网络或 Provider 无关读取失败会把已经提交的事务误报为保存失败。 | 删除三份中间映射文件及旧式 `AnalysisConfig`，活动配置和五类服务商草稿统一为 `InsightSettingsSnapshot`；后端 DTO 只在 API 边界严格解析/序列化一次，Store 只深拷贝当前模型。保存事务直接合并返回的凭据摘要并返回清除全部 API Key 的权威快照，模态直接采用该结果，不再发起提交后的重复 GET。没有旧字段读取、数据迁移或兼容分支。 | Insight API/设置/Store 7 个定向文件 115 tests、TypeScript typecheck、ESLint 及源码残留搜索通过。 | FIXED |
| AUDIT-064 | Insight 全新配置默认模型 | 全新数据库未保存过 provider setting 时，后端按共享 Provider Manifest 解析默认模型，前端新草稿却把 model 建成空字符串；用户只输入 API Key 保存会把空模型写入当前设置。 | 五类未保存草稿均直接从项目唯一的 `ai_provider_manifest.json` 取对应能力默认模型，不新增另一份硬编码、数据迁移或兼容读取。 | API 配置 10 tests 及相邻 25 tests、typecheck、ESLint 通过；全新隔离数据库的内置浏览器实测确认 Gemini 默认模型、凭据脱敏回显和单事务保存。 | FIXED |
| AUDIT-065 | Insight 任务详情进度口径 | Insight job 内部由 N 个页面 item 和 1 个发布/派生 item 组成；任务卡片已按页面池显示 `0/N`，详情却直接显示内部 item 计数 `0/(N+1)`，同一任务出现两套无说明进度。 | Insight 详情与卡片共用 `insight_analyze_page` 页面池口径，明确标为“页进度”，失败/跳过同样来自页面池；其他任务保持通用 item 计数。后端内部任务图未改动。 | 任务中心 15 tests、typecheck、ESLint 通过；隔离 API 下创建 6 页真实分析任务，内置浏览器实测队列卡、展开详情、历史卡与取消链路均一致显示 `0/6`。 | FIXED |
| AUDIT-066 | 阅读器设置、数据身份、资产失败与沉浸边界 | 阅读器设置控件虽写入本地存储和全局 CSS 变量，但画布不消费图片宽度且把间距硬编码为 8，造成设置视觉失效；路由未验证章节属于当前书籍、页面属于当前章节，也未拒绝不完整的 all-pages 响应，下一章节加载失败时还会短暂重现旧图。图片请求失败只留下浏览器破图；全局 `App` 在阅读器仍挂载任务中心并保持 SSE，违反沉浸式只读边界。 | 设置改为 `ReaderControls → ReaderView → ReaderCanvas` 的显式 typed 单向投影，画布以 scoped CSS 和真实 gap 即时消费；装载前清空旧快照并严格闭合书/章/页身份及分页完整性；资产失败保留预留几何并显示可识别错误占位。按路由 fencing 任务中心初始化，进入阅读器卸载抽屉/启动器并断开 SSE，离开后恢复；不增加旧设置迁移或兼容读取。 | 阅读器与 App 生命周期 7 个文件共 50 tests、typecheck、ESLint、Stylelint、diff check 及 2 个 Playwright 视觉用例通过；内置浏览器完成桌面、390×844、设置即时生效、错误边界和阅读器内外任务中心挂载实测。 | FIXED |
| AUDIT-067 | 多轮校对身份与凭据归属 | 校对服务商配置用数组下标构造 domain；删除中间轮次后，后续轮次的模型请求、凭据和异步状态会整体错位。删除轮次也只改设置文档，遗留当前 provider setting。 | schema-v4 要求每轮持有稳定 UUID，前端 key、控件 ID、模型发现、连接测试和设置事务全部按 UUID 归属；后端 resolver、诊断、校验与 provider 清理采用同一严格 domain。移除轮次只删除已失去 owner 的当前 provider setting；任务快照仍引用的不可变凭据版本保留，不做旧 schema 迁移。 | Backend 定向 3 tests、前端 provider/settings 相关 29 tests 通过；覆盖重复 ID、删除中间轮次、乱序模型响应及历史凭据仍可解析。 | FIXED |
| AUDIT-068 | 出厂翻译提示词与执行协议 | 全新数据库把批量普通、批量 JSON、单气泡普通和单气泡 JSON 四个槽位都写成同一条泛化文本，与执行器的两套响应解析协议以及前端出厂值不一致；高质量翻译示例还在声称严格 JSON 的代码块中放了注释，校对字段名把 `Index` 错写为 `lndex`。 | 数据库出厂文档直接使用算法层现有的四个 canonical prompt 常量；HQ、校对和 OCR 出厂提示词与共享常量统一；删除非法 JSON 注释并修正 `imageIndex`/`bubbleIndex`。仅影响全新数据，不增加迁移或兼容读取。 | 新增出厂文档与算法协议一致性测试；Backend 定向 3 tests 通过。 | FIXED |
| AUDIT-069 | 文字样式前后端边界 | 后端当前契约仅要求字号正整数、描边非负整数、行距正数，设置页、翻译侧栏和气泡编辑器却分别硬限制 512/999、10/64、3/10，并在 hydrate 时拒绝后端合法值，形成三个互相矛盾的事实源。 | 删除所有无产品依据的上限，三个界面与完整 DTO 解析统一服从后端边界：字号至少 1、描边不小于 0、行距大于 0；不改变必要的类型和下界校验。 | 前端 5 个定向文件共 41 tests 通过；新增 1024 字号、80px 描边、12.5 倍行距的保存和 schema 回归。 | FIXED |
| AUDIT-070 | 设置辅助资源恢复与字体请求竞态 | 已保存提示词请求失败后只显示不可交互错误，必须关闭重开设置；系统字体列表刷新期间仍可触发上传，上传后的刷新会被正在进行的刷新短路，目录可能暂时缺失新字体。 | 错误 chip 成为明确的点击重试入口；字体目录加载期间禁用并拦截上传入口，保证上传和目录刷新串行，继续由 settings store 持有唯一目录。 | SavedPromptsPicker/MoreSettings 新增失败重试和并发拦截测试；相关 10 tests 通过。 | FIXED |
| AUDIT-071 | 设置事务提交与插件文字默认值 | 设置事务已经成功后，前端仍追加一次完整设置读取；追加读取失败会把已提交结果误报为保存失败。插件默认文字样式又能覆盖当前统一默认值，形成第二套事实源。 | 设置事务直接采用提交响应更新当前快照，不再执行提交后的重复读取；插件只消费项目唯一文字样式默认值。未增加旧数据兼容。 | 设置事务、插件与文字样式定向测试及 typecheck 通过。 | FIXED |
| AUDIT-072 | 服务商草稿缓存与当前设置 schema | 首次切换到尚未缓存的服务商时，草稿可能继承前一个服务商的模型与选项；设置读取还会接受缺字段或旧 schema 并补默认，掩盖损坏数据。 | 未缓存服务商只从当前 manifest 建立独立默认草稿；前后端只接收完整当前 schema，旧数据要求清除，不迁移、不兼容。 | 设置 store 当前 schema/服务商切换测试与后端设置测试通过。 | FIXED |
| AUDIT-073 | 并行设置任意并发上限 | 深度学习并发控件把合法配置硬限制为 4，与后端按设备资源执行的当前契约不一致。 | 删除无产品依据的上限，只保留正整数边界；不改变并行流水线的 50 页领先窗口。 | ParallelSettings/store 定向测试与 typecheck 通过。 | FIXED |
| AUDIT-074 | 插件 Agent 独立提交与设置取消 | 插件 Agent 凭据在子弹窗中已经独立提交，但外层设置取消仍恢复打开时旧快照，之后保存还可能覆盖已提交值；成功提交后本地明文密钥也继续保留。 | 子弹窗保存后同步清除明文并通知外层仅更新插件 Agent 的已提交基线；外层取消仍回滚其他未保存草稿，不回滚独立提交。 | SettingsModal/PluginAgentModal/PluginManager 事件与取消回归测试通过。 | FIXED |
| AUDIT-075 | 提示词库类型切换失败状态 | 切换提示词类型后若新列表请求失败，界面仍显示上一类型列表，容易把旧数据误认为当前结果；选择提示词还重复写入同一状态。 | 类型切换立即清空旧列表，再加载目标类型；选择逻辑统一交给单一加载函数。 | PromptLibrary 请求失败与选择测试通过。 | FIXED |
| AUDIT-076 | 插件配置读取与替换导入 | 配置 schema 和当前值来自同一端点却被请求两次；快速切插件可被旧响应覆盖，保存可重复提交；冲突替换导入失败在嵌套异常中没有用户反馈。 | 单次读取配置文档并以请求身份约束提交；配置保存和文件导入增加唯一在途锁；替换失败统一显示真实错误；默认启用状态直接来自插件 DTO。 | PluginManager/API 定向测试与 typecheck 通过。 | FIXED |
| AUDIT-077 | 插件 Agent 会话恢复与异步生命周期 | 恢复 modify 会话时没有恢复模式；程序化 hydration 会触发选择 watcher 并删除服务端会话；关闭后旧初始化响应仍可能写回，新旧会话切换也可交叉清理。 | 恢复服务端 mode；删除 hydration 副作用 watcher，只在用户切换目标时清理捕获的旧会话；初始化和终态刷新用请求代际 fencing，规划/运行中禁用模式及目标切换。 | PluginAgentModal 28 tests 覆盖恢复、关闭重开、切换和终态刷新，typecheck 通过。 | FIXED |
| AUDIT-078 | AI 设置输入边界一致性 | 多个前端重试/RPM 控件与后端 OpenAI 选项上限不一致；连接测试允许空白密钥、半套百度凭据或缺失模型/base URL继续发请求；后端凭据校验也接受纯空白 secret，provider batch size 与领域契约不一致。 | 前端统一为 RPM 0–100000、重试 0–100，并在调用前验证当前服务商必需字段；后端拒绝空白 secret，provider batch size 收敛为 1–10。 | 前端 typecheck 与 55 tests、后端 2 个边界 tests 通过。 | FIXED |
| AUDIT-079 | 网页导入执行参数边界 | 网页导入界面、持久设置校验和任务冻结各自维护一套互相冲突的次数、并发、超时、重试、延迟及图片尺寸上限；本地当前 schema 解析又接受负数和小数，导致前端认可、后端拒绝。 | 删除无产品依据的上限，只保留整数类型、必要下界及图片质量 1–100 的固有边界；前端当前 schema 解析与后端使用同一边界，百万像素尺寸仍可通过。不增加旧配置兼容。 | Web Import Backend 18 tests、前端面板/服务商 28 tests、typecheck 与 ESLint 通过。 | FIXED |
| AUDIT-080 | 共享服务商限流锁竞争 | Provider 限流器会重试 CAS/唯一键竞争，却让瞬时 SQLite BUSY/LOCKED 直接冒泡到真实模型步骤；一次短锁竞争即可使翻译或分析失败。 | 限流事务对 BUSY/LOCKED 最多重试两次、每次短暂退避；第三次仍锁定则原样失败，不无限等待、不伪造放行，也不重启 API。 | 平台完整 74 tests 通过；覆盖两次瞬时锁后成功及持续锁第三次失败。 | FIXED |
| AUDIT-081 | 单页操作配置解析范围 | 单个气泡翻译、OCR、检测甚至纯颜色提取都会解析整条翻译流水线、术语约束和全部无关 Provider；任一无关设置损坏或缺失即可阻断本不依赖它的操作。Provider payload 还被重复深合并。 | 单页操作只读取当前页面所属章节设置，并只查询实际步骤需要的一个 Provider；颜色提取不依赖设置，检测不依赖 Provider，OCR/翻译各自冻结唯一相关段。共享 provider payload 只合并一次。 | 翻译完整 71 tests 通过；新增删除术语约束仍可单气泡翻译、删除翻译设置仍可颜色提取的回归。 | FIXED |
| AUDIT-082 | 提示词任意内容上限与重复提交 | 提示词内容被数据库之外的 20 万字符上限拒绝；保存/更新/删除也没有唯一在途锁，双击可创建重复请求或在首次成功后再显示 revision/404 错误。 | 删除内容长度上限及 OpenAPI 限制，保留名称字段真实数据库边界；提示词变更期间统一禁用相关控件并串行提交。 | 平台完整 74 tests、PromptLibrary 11 tests、typecheck 与 ESLint 通过。 | FIXED |
| AUDIT-083 | 校对全局重试死配置与 schema 事实源 | “全局重试次数”从未被执行器消费，只在新增轮次时复制一次；每轮已有独立业务/传输重试，界面因此显示第二套无效配置。翻译 schema 版本还在默认值、校验、前端 Store、章节内存和种子代码多处硬编码。 | 删除无效全局字段、UI、Store action 和冻结快照字段，新轮次直接使用唯一默认业务重试；当前翻译设置提升为 schema-v5，前后端与种子统一引用唯一版本常量，旧 schema 直接拒绝，不迁移、不兼容。 | 平台 74、内容 61、翻译 71、前端设置相关 50 tests，typecheck 与 ESLint 通过。 | FIXED |
| AUDIT-084 | 约束表行身份 | 约束表在筛选或排序后使用显示序号作为 Vue key，同一 DOM/焦点状态会被复用给另一条原始记录。 | key 改为原数组索引，排序和筛选时仍保持记录身份；编辑和删除继续使用同一 originalIndex，不引入额外行 ID 或迁移字段。 | TranslationConstraintTable 9 tests、typecheck 与 ESLint 通过。 | FIXED |
| AUDIT-085 | 批量导入冻结配置复杂度 | 容器导出与网页导入 Worker 每处理一页都重新遍历整个冻结条目数组，2000 页任务形成 O(n²) 重复校验开销。 | 完整配置只在任务创建或冻结路径发布边界校验一次；逐页执行仅校验当前 step kind 和映射类型，不增加数量上限。 | Transfer/Web Import 22 tests 通过；新增回归证明逐页读取不遍历全表。 | FIXED |
| AUDIT-086 | 插件运行时加载与失败语义 | 插件动态导入会并发修改进程全局 `sys.modules`/`sys.dont_write_bytecode`；导入时内存分配失败可被 continue 策略降级为普通插件错误；损坏的当前 JSON 又会默认成空对象。 | 用进程级可重入锁串行模块发布和缓存释放；MemoryError/CUDA/原生分配失败立即使步骤失败；当前 JSON 必须为合法对象，不做旧数据回退。 | Plugins 46 tests 通过，覆盖导入时 MemoryError 与损坏 JSON 拒绝。 | FIXED |
| AUDIT-087 | 插件包文件和 SQLite 事务边界 | 插件刷新在 `BEGIN IMMEDIATE` 内扫描、校验并哈希整个包，长时间占有 SQLite 写锁；解压中途失败遗留 staging，删除事务已提交后清理失败又会恢复包文件，造成文件/数据库不一致；哈希和建包还会整文件入内存。 | 包扫描和校验移到写事务外，事务仅更新状态；解压全程 `finally` 清理；已提交删除的后续文件清理失败不再回滚成孤儿包；哈希和 ZIP 改为流式。 | Plugins 46 tests 通过，覆盖部分解压失败、刷新内存错误和提交后清理失败。 | FIXED |
| AUDIT-088 | 插件命令伪幂等与整数边界 | 插件配置、默认启用和删除端点强制客户端生成 Idempotency-Key，但后端从未存储或消费，形成无效协议；revision 解析还会把小数截断为整数。 | 仅保留真正具有幂等记录的插件导入端点，其余端点删除伪幂等头；正整数只接受整数或十进制整数字符串。 | Backend Plugins 46 tests、前端 Plugin API 62 tests、API 生成与 typecheck 通过。 | FIXED |
| AUDIT-089 | 插件 Agent 会话限制与并发状态 | Agent 服务人为限制全局只能有一个规划会话、消息最多 10 万字，与已按 session ID 隔离的设计矛盾；同一会话又可重复并发规划/启动，空快照被误当成未提供，Agent 工具还可吞掉内存错误。 | 删除无产品依据的全局单会话与消息字数上限；用 session 级规划/启动状态阻止重入；只有 `None` 表示读取当前快照，空快照保留真实语义；当前 JSON 严格解析，内存失败立即上抛。 | Plugins 46 tests 通过，覆盖多规划会话、大消息和 Agent 失败语义。 | FIXED |
| AUDIT-090 | 插件版本替换配置语义 | 安装新版本时会挑选同名字段继承旧配置，属于显式的旧数据兼容；替换配置文档又没有推进 config revision，已打开的旧页面仍可用旧 revision 覆盖新 schema。 | 插件版本替换直接采用新 schema 默认配置，旧配置全部舍弃，不迁移、不保留同名字段；同一事务推进 config revision。 | Plugins 46 tests 通过；覆盖旧值舍弃、新默认值和 revision 推进。 | FIXED |
| AUDIT-091 | 网页导入失败记录边界 | 下载异常原文会未脱敏写入草稿页；记录失败状态时再次出错会遮蔽原始下载失败；批处理器还会把 MemoryError 当成普通页错误后返回整批成功。 | 持久化和任务失败消息统一脱敏；失败日志写入改为非权威最佳努力，不遮蔽原始异常；MemoryError/CUDA/原生分配失败跳过附加记录并立即向批处理边界传播。 | Web Import 与 Plugins 组合 67 tests 通过，覆盖批处理内存错误与失败记录二次异常。 | FIXED |
| AUDIT-092 | 插件当前契约与原子输出边界 | 插件配置 schema 允许缺默认值、标量 select 选项和未知元数据，前端还把旧 `minimum`/标量选项归一成当前形状；原子 hook 输出只拒绝未知顶层字段，却可删除必需字段、改变气泡数组长度或篡改文档 revision；资产发布还访问不存在的 64MiB 常量，任何调用都会抛 `AttributeError`，长普通文本又会被 base64 猜测误拒。 | 配置、manifest、hook 输入输出统一为严格当前结构，所有配置字段必须给默认值；删除前端旧形状归一化；插件链必须保持页面 ID、数组形状和文档 revision；资产发布移除无依据且失效的大小门槛；只拒绝明确的二进制/data URL，不猜测普通字符串。 | Backend Plugins 51 tests、Plugin API/Manager 25 tests、OpenAPI 生成、typecheck 与定向 ESLint 通过。 | FIXED |
| AUDIT-093 | 持久层事实约束与基础建表一致性 | 插件 ID 契约允许 100 字符但三张表仍声明 UUID 长度 36；续写图片/形态图片可同时出现多个 active/adopted 版本；后加的 revision、ordinal、schema version、页码与图片尺寸缺少与既有表一致的 SQLite 约束。原“exact schema”测试又没有比较 SQLite `CHECK` 集合，漏约束仍会通过。 | 当前模型和唯一 foundation 同步使用 100 字符插件 ID；补齐必要的正数/形状约束与两个局部唯一索引；资产服务要求宽高成对；foundation 直接重写，不增加迁移或兼容；测试逐表比较迁移库与 metadata 的全部 `CHECK` 名称。 | Storage contracts 12 tests、资产平台用例 17 tests、foundation upgrade/check/downgrade、py_compile 与 diff-check 通过。 | FIXED |
| AUDIT-094 | 桌面控制中心当前契约与生命周期 | 桌面任务中心仍给实时队列附带历史上限，重启后旧命令/重试可回写新服务；请求可无限挂起。设置和桌宠 manifest 接受额外字段/布尔坐标；显式停止期间改设置会意外重启，无系统托盘时关闭窗口会失去入口。暂停任务在概览消失，日志视图无真实行数上限，隐藏桌宠仍持续动画。 | 实时队列不再限量，历史仍取最近 200；所有延迟与命令响应按服务代次隔离并设置有限请求超时。设置/manifest 只接收当前精确结构；显式停止优先、降级态提供重启、无托盘时走正常退出。概览保留暂停任务，日志文档与缓存同限 5000 行，隐藏桌宠停表并在显示前恢复位置。 | 桌面设置、桌宠、图标、状态机和 GUI 共 41 tests 通过；compileall 通过。 | FIXED |
| AUDIT-095 | 子进程 epoch 注册与启动诊断 | 进程表新增 `pid >= 1` 后与 Launcher 的当前两阶段协议冲突：API/Worker 必须先以 `pid=0` 注册 epoch，成功 spawn 后再绑定真实 PID；因此所有真实 Launcher 启动都在 spawn 前失败，且注册位于异常日志边界外，只显示笼统的连续重启。 | 当前事实约束改为 `pid >= 0`，仓储明确拒绝负数/布尔值；注册纳入子进程启动异常边界，保留 0 作为“尚未绑定”的当前状态，不是旧数据兼容。 | epoch 仓储回归、Storage contracts 12 tests、Launcher 健康启动及 API/Worker 自隔离重启 3 个 Windows 真进程用例通过。 | FIXED |
| AUDIT-096 | PyInstaller 隐式导入清单 | `app.spec` 仍列出重构中已删除的 Manga Insight `base_client`、配置序列化和 JSON parser，打包契约直接失败并可能误导后续依赖维护。 | 删除已不存在的模块及其空父包隐式导入，只保留当前实际复用的配置模型、传输客户端和图像生成客户端。 | `test_production_spec_has_no_missing_project_hidden_imports` 通过。 | FIXED |
| AUDIT-097 | 翻译页文档 CAS 与冲刷生命周期 | 页面文档保存入口接收 `documentRevision` 却完全忽略，旧 watcher 可把过期编辑排入新注册文档；在网络写入期间请求 flush 且该批次直接结束时，`flushRequested` 又会残留，使下一次编辑绕过 150ms trailing debounce。入口还会在确认文档已注册前给调用方气泡写入 mutation ID。 | 保存入口先验证当前已注册文档及精确正整数 revision，再写入客户端 mutation ID；持久化循环无论成功失败都清理 debounce/flush 状态，保留当前单一 CAS 协调器，不增加重放兼容层。 | stale revision、调用方不被污染、在途 flush 后恢复 debounce 的回归用例通过；翻译页定向 48 tests 与 ESLint 通过。 | FIXED |
| AUDIT-098 | 翻译页上下文、编辑态与键盘边界 | 新章节 bootstrap 等待期间不会立即使旧页面读取失效，旧响应可先回写；旧初始化在 `switchImage` 被更新请求取消后还可能清空新上下文。无当前页时仍可进入编辑态并把页面变为空白；当前页被清空后编辑态也不会退出。全局快捷键对空 target 会崩溃、会劫持 select/button/可编辑后代，字号下调还保留与当前契约冲突的 10px 下限。 | 章节刷新开始即中止并递增页面请求代次，初始页 await 后再次核对章节代次；没有稳定当前页时不进入编辑，当前页消失即退出；快捷键按真实交互祖先过滤、异步导航显式丢弃 Promise，并统一 1px 下界。删除无模板消费者的提示样式和未使用 transition 项。 | 页面读取/章节刷新乱序、空页面编辑态、表单快捷键及 1px 边界回归通过；翻译页定向 48 tests 与 ESLint 通过。 | FIXED |
| AUDIT-099 | UI 架构检查器 Vue 语法边界 | CSS ID 规则直接扫描整个 `.vue` 文本，把标准的具名插槽缩写 `#footer`/`#actions` 误判为 CSS ID；因此全量架构审计会在合法的问答消息组件上失败，诱导业务组件改写正常模板语法。 | ID 选择器规则只扫描提取出的 `<style>` 内容；CSS 文件仍扫描全文。未放宽 ID 禁令，也未给具体业务组件加白名单。 | 新增“模板具名插槽允许、style 中 ID 拒绝”双向回归；架构检查器扩展至 89 tests，与全仓 `lint:ui:audit` 通过。 | FIXED |
| AUDIT-100 | Product 通用组件交互与语义边界 | 文本组合器和搜索框会把中文输入法确认回车当作提交；choice radiogroup 与 segmented tab 虽声明标准 ARIA 角色，却只改值不移动焦点。可选图片卡把自带 label 的 UiCheckbox 再嵌入外层 label，形成无效 HTML；文件拖拽跨子元素会误退出高亮。阅读器不会挂载全局任务中心，却仍为其保留 136/72px；分栏拖动不处理取消/失焦且右键也能启动。外链还完全依赖消费者手写 noopener，缩略图数字徽标 0 被当作空值。 | 输入组件尊重 `isComposing`；单选卡/标签实现跳过 disabled 的 roving focus；图片卡改为同一 input 的两个并列 label；dragleave 只在真正离开 owner 时结束。仅 brand/fixed 页眉为实际存在的任务入口留位；拖动限定主键并在 pointercancel/blur 清理；`_blank` 外链默认安全 rel；显式 0 徽标正常显示，disabled 卡不再呈现 hover。 | Product 相关 4 个文件共 78 tests、目录级 ESLint 与 Stylelint 通过。 | FIXED |
| AUDIT-101 | 基础 UI 控件当前交互与冗余契约 | 两套自定义选择器只能用键盘开关菜单，无法移动/确认选项；Combobox 还会选择 disabled 项，并以不可被 `<label for>` 关联的 div 充当控件。通用 tone 规则会覆盖实心主按钮的反色文字；图标宣称支持 CSS 字符串尺寸却把 `1em` 强转为 `NaN`；小数步进产生浮点尾差，非空数值框清空时又会立即写 0。组件层还保留无消费者的 UiInput 布尔分支、UiField 双别名、文件输入 DOM 镜像属性、Combobox 变体、Sidebar mode、hidden 颜色框样式和 selector token。 | 选择器保持现有视觉边界，以原生 button 作为 trigger，并实现 active-descendant、跳过 disabled 的方向/Home/End/Enter/Space 键盘模型；架构门禁只允许四个基础按钮/选择器 primitive 拥有原生 button。实心按钮不再被普通前景 tone 覆盖，图标分别处理数值与 CSS 尺寸，数字步进归一浮点结果并保留空白输入草稿。直接删除所有无当前消费者的兼容接口/token，不增加替代层。 | UI primitives 54 tests、架构门禁 89 tests、全量 vue-tsc、ESLint、Stylelint 与 `lint:ui:audit` 通过。 | FIXED |
| AUDIT-102 | 长列表缩略图虚拟尺寸与越界窗口 | 右侧纵向缩略图固定用 164px 作为步长，但真实卡片随 180px 容器按 3:4 渲染，每项连间距为 246px；滚动越深，虚拟偏移与 DOM 的误差越大。网格的可配置 gap 从未传给真实网格，两个 visibleChange 事件也无消费者。集合缩短后若保留旧 scrollTop，固定窗口还会得到 `start > end`。 | 纵向步长直接由 ResizeObserver 同步的真实容器宽度和当前 3:4 卡片契约计算；网格统一使用真实 6px gap，并扣除末尾不存在的额外间距。删除未使用的 itemHeight/gap/visibleChange/暴露方法；固定窗口把陈旧滚动位置钳到最后一个有效项。 | 1000 页 DOM 窗口、响应式 246px 步长、选中项滚动及缩短集合边界共 8 tests，ESLint 与 Stylelint 通过。 | FIXED |
| AUDIT-103 | 附加请求 JSON 编辑器实例与草稿所有权 | 每个编辑器都硬编码 `openAiExtraBody`，校对多轮同时渲染时会生成重复 id/label 关联；用户刚输入一段合法 JSON 后，v-model 回写又立刻把文本自动格式化，导致光标跳动，实际“格式化”按钮失去明确边界。 | 每实例用 `useId` 生成当前唯一控件 id，并允许显式覆盖；只跳过由本实例发出的同一原始对象回写，外部真正变更仍同步，格式化只由用户按钮触发。 | OpenAI options 12 tests、vue-tsc 与 ESLint 通过，覆盖同页双实例和合法草稿不被改写。 | FIXED |
| AUDIT-104 | 任务入口首屏失败与终态摘要 | 批量分析弹窗若以打开状态首次挂载不会加载书籍，加载失败还成为未处理的 watcher Promise；任务徽标在快照加载后只保留 summary 的 failed，历史列表截断时 interrupted 会消失。赞助二维码在窄屏固定为 400px 以上横排。 | 打开态 watcher 立即运行并将书籍读取失败呈现在弹窗内；徽标对 failed/interrupted 取实时投影与持久摘要的较大值；二维码保持桌面尺寸并允许换行，窄屏降为 150px。同步修正任务测试中漏掉当前必需 `cancelled` 字段的陈旧 fixture。 | Task Center 9 tests、Sponsor 4 tests、vue-tsc、ESLint 与 Stylelint 通过。 | FIXED |
| AUDIT-105 | 网页导入日志状态语义与下载反馈 | 通用日志面板把 info 与 warning 都渲染成警告色，并用主操作浅色表示 success；网页导入又把 info 映射为 warning、tool call 映射为 accent、thinking 映射为 warning，五类日志无法稳定辨认。下载按钮同时让 spinner 和可见文案播报“下载中”，形成重复读屏反馈。 | 日志面板按 info/success/warning/danger/accent 的现有语义令牌分别着色；网页导入将信息、工具调用、工具结果、思考、错误映射为对应的五种 tone；按钮内 spinner 保持装饰性，由唯一可见文案承担状态名称。 | Web Import panels 24 tests、定向 ESLint 与 Stylelint 通过。 | FIXED |
| AUDIT-106 | 网页导入模态框异步会话与状态同步 | 创建草稿、恢复草稿或提交入库的旧响应可在窗口关闭乃至重新打开后继续回写，并可能关闭新会话；任务事件密集时多个草稿读取并发，旧 revision 可覆盖新事实。分页请求也可跨草稿清除新 loading，加载失败成为未处理 Promise；完全依赖 SSE，漏掉事件后界面永久停在处理中。 | 用单一会话代次约束创建、恢复、选择和提交；已被后端接受的陈旧提交只通知任务接收，不再修改新窗口。草稿读取合并为单一串行 runner，分页使用请求身份；任务事件保持即时同步并增加一个 1 秒只读兜底轮询，瞬时状态读取失败不伪装成任务失败。日志事件按 ID 去重并完整分页，严格拒绝未知日志类型。 | `useWebImportModal` 13 tests、WebImportModal 7 tests、typecheck 与定向 ESLint 通过；覆盖关闭后回写、首次读取失败恢复和分页失败。 | FIXED |
| AUDIT-107 | 网页导入设置事务与当前输入契约 | 设置事务成功后又发一次 GET；该 GET 若瞬时失败，前端会误报保存失败、恢复旧 revision，下一次保存必然冲突。保存期间输入仍可修改并被返回值覆盖。自定义 Headers 虽标为 JSON，却保留逐行旧格式、静默跳过坏行并把任意值 `String` 化；Agent 的重试/超时是执行器真实字段却没有编辑入口。选择更新写端点还遗漏全局写访问门。 | 直接消费事务返回的设置/Provider revision 与凭据摘要，不再二次读取；成功后清除前端 secret 草稿并锁住整份保存中的 fieldset。Headers 只接受当前非空字符串 JSON 对象并反馈具体错误；补齐 Agent 重试/超时控件且不设任意上限；选择更新使用生成命令类型和写访问门。 | Store 6、Panels 25、API 4 tests，合计相关 108 tests、typecheck、ESLint 与 Stylelint 通过。 | FIXED |
| AUDIT-108 | 网页导入当前 UI 投影与弹层契约 | 后端当前草稿不再提供漫画/章节标题，但前端保留 `success/title/sourceUrl` 旧结果字段并渲染成空标题；ready 草稿缺实际引擎时又默认冒充 AI Agent。支持提示完全忽略用户显式选择的引擎。免责声明独占的 popover 层让普通 dialog 越过任务中心，并迫使 BaseModal 保留唯一的层级分支；中文输入法确认回车也可能直接同意。 | 结果投影只保留当前页面、总数和已解析引擎，标题统一为“提取结果”，实际引擎缺失立即报契约错误；支持提示按 auto/Gallery-DL/AI Agent 分别计算，显式 Agent 不再发无用支持检查。免责声明回归普通 modal 层并尊重 IME composition，删除 BaseModal 无剩余消费者的 overlayLayer；同步修正质量标签与提取 spinner 读屏。 | BaseModal 18、Disclaimer 5、Results 8、Panels 25、Composable 13 tests 通过；typecheck、ESLint 与 Stylelint 通过。 | FIXED |
| AUDIT-109 | 翻译结果显示与导入导出状态 | 结果文本仍按 60 个字符写入人为换行；图片大小滑块虽标 50%–200%，但 flex 收缩和 `max-width:100%` 让 100% 以上完全无效。导出期间仍能启动当前图下载、文本导入/导出；文本导入又可并发提交，已接受任务后的任务中心刷新失败会被误报为导入失败。 | 文本只保留原始换行并交给 CSS 自然折行；图片层禁止 flex 收缩且容器横向滚动，使 50%–200% 均真实生效。导入与导出共享互斥 UI，文本导入自身串行；先验证后端任务 ID，再把任务中心刷新降为非权威同步，成功提交不被后续刷新失败推翻。 | ImageResult 17、Export/Import 6 tests 及相关组合通过；typecheck、ESLint 与 Stylelint 通过。 | FIXED |
| AUDIT-110 | 图片上传章节身份与缩略图/任务状态投影 | 两个文件入口可在同一事件窗口启动重叠上传；上传过程中切换章节后，后续文件和失败重试会读取新的 `props.chapterId`，旧批次完成又会刷新新章节。文件夹缩略图逐项 `findIndex`，2000 页形成 O(n²)；已手工标注的处理中页面还会被“标”覆盖，失败提示错误声称点击即可重试。暂停任务仍保持进度动画，取消项也未计入池终态比例。 | 上传开始时冻结章节 ID、拒绝第二个在途批次，进度/结果/重试只回写同一章节；切章清除旧失败列表。缩略图用一次计算的 ID→索引表，处理中状态优先于标注，提示只陈述真实动作；当前文件夹消失时回到根目录。暂停时停止动画，取消数进入可见摘要和终态比例。 | ImageUpload 9、Thumbnail 9、FolderTree 3、TranslationProgress 7 tests 及 96 条翻译组合用例通过；无图片数量限制或迁移层。 | FIXED |
| AUDIT-111 | 翻译侧栏工作流与弹窗生命周期 | 术语/禁翻弹窗同时用本地 v-model、watch 和 close 事件镜像同一可见状态，一次关闭向父级回传两次。快速工作区弹窗首次以打开态挂载不加载书架，加载失败无界面反馈，IME 回车会误提交。侧栏又允许后端批量任务运行时删除/消字，并把页面文档尚未加载错误地扩散成所有纯后端工作流不可用；字体上传成功后追加 GET，GET 失败会假报上传失败。 | 弹窗只保留父级唯一可见状态，保存中阻止用户关闭但成功路径明确关闭；快速保存 watcher 立即加载并显示错误、尊重 IME。所有冲突工作流统一服从批量任务忙碌态，而纯后端工作流不依赖当前页面文档；字体上传直接用成功响应更新资源目录。同步修复非 Element 外部点击和空批量应用。 | Sidebar/Modal/Property 相关 79 tests 及扩展 96 tests、typecheck、ESLint 与 Stylelint 通过。 | FIXED |
| AUDIT-112 | 编辑工作区交互、异步状态与大图内存边界 | 编辑器快捷键会劫持表单控件；日语键盘同时渲染四组按键；气泡点击和零位移交互会重复写入/重渲染。渲染轮询读取整页摘要且允许过期响应在卸载或新请求后覆盖状态，文本样式轮询也可被同页旧响应反写。画笔预览按原图尺寸分配画布，大图会产生高额并发内存；页面切换只对任意 4K 阈值图片重新适屏，普通图片可能沿用上一页偏移。工具栏忙碌态、窄屏布局、气泡空态/焦点和取消请求语义也不完整。 | 快捷键只处理非编辑目标；日语键盘只挂载当前分组；气泡仅在真实几何变化时提交，统一由工作区拥有绘制。轮询改用轻量 render-status、有限等待、AbortController 与请求所有权校验，状态映射区分 pending/processing/failed；文本样式按页取消旧刷新。画笔预览只占视口尺寸且操作串行；所有原图切换按真实视口适配，不设图像尺寸阈值。补齐忙碌互斥、响应式堆叠、真实错误反馈、Store 快照隔离并删除无消费者的旧 edit-mode composable。 | 编辑域 26 files / 163 tests、UI 架构 89 tests、typecheck、ESLint、Stylelint、UI architecture audit 与 diff-check 通过。 | FIXED |
| AUDIT-113 | 窄屏工作区活动标签可见性 | 共享横向标签栏允许滚动，但活动标签变化或视口收窄时不主动将其滚入可视区；Insight 已选“时间线”后切到 390px 宽度会被右侧笔记入口遮掉部分活动标签。 | 通用标签工作区在活动项变化、首次挂载及视口尺寸变化后仅对当前活动标签调用最近边界滚动；保留原横向滚动与消费者槽位结构，不增加业务特例。 | 内置浏览器 390×844 实机复核通过；共享工作区 21 tests、typecheck、ESLint、Stylelint 通过。 | FIXED |
| AUDIT-114 | 角色工坊入口窄屏布局 | 漫画分析中的角色工坊入口卡片在 390px 视口仍保持桌面双列网格，标题和说明被压成近乎逐字换行，操作按钮也挤占正文空间。 | 在现有记录卡响应式变量上将窄屏入口改为单列三行，正文、状态和操作依次排列；不复制卡片结构或新增移动端组件。 | 内置浏览器 390×844 实机复核通过；入口组件 3 tests、typecheck、ESLint、Stylelint 通过。 | FIXED |
| AUDIT-115 | 概览最近错误卡片窄列可读性 | 桌面双栏概览中“最近分析”列本身较窄，错误横幅仍把图标、长错误正文和重试按钮强制放在一行，正文被压成竖向碎片。 | 仅在最近分析错误 owner 上使用现有状态横幅布局变量改为纵向，操作区占满卡片宽度；不改变共享状态组件或其他页面。 | 内置浏览器桌面错误场景复核通过；概览 17 tests、typecheck、ESLint、Stylelint 通过。 | FIXED |
| AUDIT-116 | 笔记加载、失败与空态混显 | 初次读取笔记时加载/错误横幅后仍渲染空列表，界面同时声称“读取失败”和“暂无笔记”；但若已有列表后的分页或编辑操作失败，又不能因此隐藏现有笔记。 | 初次加载或无数据失败时不挂载列表；已有笔记后的操作失败继续保留列表，只隐藏不可继续执行的加载更多入口。状态仍由唯一 Store 投影，不增加本地镜像。 | 内置浏览器加载、空、错误场景复核通过；笔记面板 8 tests、typecheck、ESLint、Stylelint 通过。 | FIXED |
| AUDIT-117 | 全局设置表单状态所有权 | 翻译、OCR、高质量翻译、检测、更多和文本默认值组件把 Store 的事务草稿再次复制到本地对象，并用 36 个 watcher 或可写 computed 双向同步；后端重新装载整份设置或切换 Provider 后，本地副本可继续显示并写回旧值。翻译页还在挂载时把翻译模式/JSON 开关固化为常量。 | 所有业务控件直接读取唯一 Store 草稿并显式调用既有模块 action；文本默认值直接编辑父弹窗事务草稿；并行设置补一个所属模块的最小 patch action。只保留模型列表、请求状态和 OCR 专用提示词语言等真实 UI 临时状态。 | Store 整体替换回归覆盖检测、翻译、OCR、高质量翻译；设置相关定向 100+ tests、typecheck、ESLint、Stylelint 与 UI architecture 通过。 | FIXED |
| AUDIT-118 | 字体上传成功契约与错误语义 | 后端字体 POST 只返回不完整的 ID/URL；两个设置入口上传成功后追加 GET，若 GET 瞬时失败会把已成功写入的字体误报为上传失败；翻译侧栏则自行拼出另一种 FontResource，三个入口的权威结果不同。 | 字体 POST 直接返回完整当前 `FontResource`，三处前端统一消费响应并 upsert 唯一字体目录，不追加 GET、不合成兼容对象；同步 OpenAPI 与生成客户端。 | 后端精确路由测试、字体相关 72 个前端测试、typecheck、ESLint、Stylelint 与确定性生成通过。 | FIXED |
| AUDIT-119 | 设置弹窗首次打开与保存关闭竞态 | 真实 BaseModal 只在 `modelValue` 变化时发 open，SettingsModal 若首次以打开态挂载会永远停在加载界面；保存事务进行时仍允许 Esc、遮罩、关闭和取消，先回滚本地后服务器却可能成功保存旧草稿，造成前后端分叉。 | SettingsModal 自己立即监听外部开关并拥有装载请求；保存期间统一关闭四条用户退出路径，成功后走已授权关闭，失败则保持草稿和窗口。 | 真实 BaseModal 首次打开集成测试、延迟保存关闭锁测试及弹窗/文本默认值 28 tests、typecheck、ESLint、Stylelint 通过。 | FIXED |
| AUDIT-120 | AI 校对设置写边界与请求清理 | 校对轮次虽没有完整本地镜像，但所有字段直接 `v-model` 写入 Store 嵌套对象，绕过现有 `updateProofreadingRound` action；按轮次懒建的模型发现请求在组件卸载时也未统一失效。 | 所有轮次字段经现有 action 更新，Provider 切换在一次 patch 中清理所属凭据/模型；卸载时失效并清空全部轮次模型请求。 | 校对 Provider、当前 schema 与 OpenAI extraBody 共 37 tests、typecheck 通过。 | FIXED |
| AUDIT-121 | 提示词目录加载与写后事实 | 提示词列表读取失败被渲染为“暂无提示词”；创建、更新、删除成功后又整表读取，后续瞬时 GET 会把已成功写入误报为失败；前端还拒绝后端允许的空内容。 | 读取失败单独显示可重试错误；写操作直接消费后端返回资源并局部 upsert/filter，不追加 GET；仅保留后端真实的名称必填边界，空内容按当前契约允许。 | PromptLibrary/SavedPromptsPicker 18 tests、typecheck 通过。 | FIXED |
| AUDIT-122 | 插件 Agent 状态所有权、会话命令与事件恢复 | 子弹窗把 Store 草稿完整复制为第二份状态；独立保存会用局部设置文档覆盖全局凭据摘要和 revision 缓存。活动会话任意读取失败都会被当成 404 丢弃；任务事件只补 200 条，长任务可漏终态。模式/目标切换忽略旧会话删除失败，锁定、启动、取消和清理可重复提交，补拉失败还残留实时订阅。 | 控件直接读唯一 Store 草稿并经模块 action 更新；独立保存只提交 translation/plugin_agent 并合并权威结果，不触碰其他域。仅 404 清除会话 ID，任务事件按后端最大页完整翻页；所有会话命令串行，清理失败保留原会话，事件补拉失败立即解除订阅。 | Plugin Agent API/Modal/Store 44 tests（设置域组合 104 tests）、typecheck 通过，覆盖瞬时失败、千条分页、重复命令与订阅释放。 | FIXED |
| AUDIT-123 | 插件管理权威资源与异步竞态 | 插件对象已有 `defaultEnabled`，组件/API 仍维护第二张默认状态表；列表失败显示为空。开关、默认状态、删除、刷新和配置保存可交叉执行，卸载后的刷新仍可能写 UI；权威列表也不清除已删除插件的 CAS 缓存。 | 删除默认状态镜像，所有局部写直接消费后端 Plugin 资源；错误态可重试。按插件串行命令，配置保存锁住关闭路径，刷新与列表读取按代次隔离并在卸载失效；权威列表清理不存在插件的 revision 缓存。 | PluginManager/API 28 tests、typecheck 通过，覆盖空/错态、刷新卸载、导入刷新和陈旧 CAS。 | FIXED |
| AUDIT-124 | 全局任务中心路由生命周期 | 普通页面首次初始化任务中心时若快速进入阅读器再返回，较早的异步初始化完成后会因过期生命周期编号调用 `disconnect()`，把新普通页面已经建立的 SSE 连接关闭。编号只表示调用顺序，无法表达连接归属。 | 删除错误的调用编号判断；异步初始化完成时只依据当前事实（App 是否仍挂载、当前是否阅读器）决定是否断开。阅读器仍立即关闭抽屉和连接，卸载仍统一释放。 | 新增“普通页→阅读器→普通页，旧初始化最后完成”回归；App/TaskCenter 18 tests、typecheck、ESLint 通过。 | FIXED |
| AUDIT-125 | AppShell 公共边界与共享 SSE 解析 | AppShell 仍公开无生产消费者的 overlay 插槽和 `immersive` chrome，阅读器又用两个等值 prop 表达同一页眉高度。共享 SSE 解析器对完整协议行 `trimEnd()`，会破坏 JSON 字符串末尾空格；正常 EOF 没有额外空行时还会丢掉最后一个完整事件。 | 删除无消费者的 shell 插槽/chrome 值；页眉高度同时驱动 header 最小高度和 fixed 内容高度。SSE 仅剥离 CRLF 的 `\r`，保留 data 内容，并在 EOF 解码尾字节、分派完整尾事件。 | 五路由消费者搜索；AppShell/App/Reader/SSE/三类调用方 118+ tests、typecheck、ESLint、Stylelint 通过。 | FIXED |
| AUDIT-126 | 翻译任务并发配置与前端生命周期 | 设置层已允许任意正整数深度学习并发，但任务规格、持久化解码和 Worker 仍拒绝大于 4，导致合法设置生成的任务失败。前端只把多页任务视为在途，单页任务及接口响应前可重复创建；请求期间切页后终态刷新还会把选择拉回旧页，提交后再读取实时设置可能展示错误执行模式。 | 三层后端契约统一为正整数，不改变 50 页领先窗口；翻译会话用一个最小在途锁覆盖创建、执行和终态回填，删除无消费者的结果包装。终态列表返回后按最新页面选择恢复并读取该页文档，进度使用提交前冻结的执行模式。 | 翻译/Store/侧栏 51 tests、后端并发 3 tests、typecheck、全量 ESLint 与 Stylelint 通过。 | FIXED |
| AUDIT-127 | 内容查询、Operation 事件和任务命令 API 边界 | 页面列表把显式 `cursor=0`/`limit=0` 当作未提供，翻译 bootstrap 又把不完整书籍身份静默降级为快速工作区；Operation SSE 未核对标准 `id` 与 JSON `eventId`，共享解析器还会破坏尾空格/丢失 EOF 尾事件；暂停、恢复、继续、取消四个任务命令未编码路径身份。 | 显式参数一律原样交给后端权威校验，bootstrap 的 book/chapter 分别转发，不在浏览器选择另一工作区；SSE 保留内容并核对 durable id/type/顺序；四个任务命令统一 `encodeURIComponent`。同步收紧 Operation `createdAt` 非空契约。 | API/SSE/任务中心 76 个定向测试、OpenAPI 连续两次生成哈希一致、typecheck、ESLint、Stylelint 通过。 | FIXED |
| AUDIT-128 | 书籍翻译约束当前 schema、CAS 与错误传播 | 前端把严格 v2 约束文档再经过旧 `non_translate`、布尔强转、默认补齐、非法条目丢弃等兼容归一化；revision 藏在 API 模块缓存，保存经通用书籍更新并产生冗余 GET；Store 又把 CAS/校验异常吞成布尔值，旧响应可覆盖已切换的书籍。 | `BookTranslationConstraints` 直接引用 OpenAPI 当前 payload，删除全部旧字段兼容和身份映射；bootstrap 将 payload/revision 一次交给 Store，专用约束 PUT 直接提交一次 CAS。保存异常原样传播，按 book/revision fencing 晚到响应，弹窗展示后端真实错误。没有迁移或兼容读取。 | 约束/API/弹窗 33 个定向测试与 typecheck 通过，覆盖无冗余 GET、revision 更新、冲突传播、切书晚到响应和未加载拒绝。 | FIXED |
| AUDIT-129 | Provider 前端预检中的残余双路径 | 当前 Provider ID 已由设置 schema 和共享 manifest 约束为 canonical 小写值，但翻译前预检仍先按原值查一次凭据，再按规范化值查一次，形成无实际消费者的兼容分支。 | 凭据查询只使用共享 manifest 的 canonical provider ID；保留前端即时表单提示，最终配置和服务商校验仍由后端负责。 | Provider、模型发现、翻译预检与属性测试 27 tests 通过。 | FIXED |
| AUDIT-130 | 当前设置前端辅助层的静默纠错 | OpenAI 选项、混合 OCR 和文字样式已经由当前设置 schema 严格约束，前端辅助函数仍会把字符串转为数字/布尔值、夹断范围、补默认值或把非法值改成另一套配置，导致损坏的当前数据被伪装成合法设置。 | 删除兼容归一化和标量强转；OpenAI 选项只在显式新建时应用工厂默认，克隆和反序列化保持当前完整结构；混合 OCR 仅保留真实的主辅引擎业务不变量，文字样式只接受真实数值。Provider 稀疏覆盖仍按后端当前正式契约保留，不做迁移。 | OpenAI、Hybrid OCR、文字样式及设置组合 13 个定向用例，连同角色工坊组合共 45 tests、typecheck、ESLint 通过；字符串字号现明确拒绝。 | FIXED |
| AUDIT-131 | 翻译约束文件导入的坏行容忍 | JSON 导入会静默丢弃非对象行，再把任意单元格用 `String` 转成文本；XLSX 也缺少精确行号，用户无法判断原文件错误，坏数据可在不知情时进入当前文档。 | JSON/XLSX 每一行都必须是对象且每个已知单元格必须是字符串，错误直接报告真实文件行号；不增加旧格式读取或数据迁移。 | 约束表 10 tests 通过，覆盖 null 行、数值单元格、排序后稳定行身份和导入导出。 | FIXED |
| AUDIT-132 | 角色工坊 API 命令类型重复与生成区段漂移 | Studio facade 为 OpenAPI 已生成的更新、会话、消息与生成命令重新手写局部结构；生成 section 又在 View、Store、编辑器和子组件之间退化为任意字符串，后端契约变化无法由类型检查发现。 | API 直接引用生成命令 schema；建立唯一的当前生成区段联合类型并贯穿 View、Store、编辑器和 Hero 子组件，候选列表也使用生成响应类型，不增加适配层。 | Character Studio API 13 tests、源码契约 9 tests，组合 45 tests、typecheck 与 ESLint 通过。 | FIXED |
| AUDIT-133 | 下载错误响应的合法 JSON 边界 | 公共下载辅助函数在 `JSON.parse` 后直接读取 `.error`，当代理或后端返回合法的 `null`、数组或其他 JSON 标量时会抛出前端 `TypeError`，遮蔽真实响应正文。 | 先确认响应是非数组对象且拥有 `error` 字段，再读取当前 v2 错误信封；其他 JSON 和纯文本按原始正文呈现。 | `apiDownload.spec.ts` 4 tests 与 typecheck 通过，新增 `null` JSON 回归。 | FIXED |
| AUDIT-134 | 文字样式工厂默认值的重复入口 | `textStyleFactoryDefaults.ts` 再次读取并归一化已经由 `textStyleDefaults.ts` 严格拥有的同一 JSON，设置组件重置时又做第二轮归一化，形成无业务职责的包装层和双重校验。 | 删除重复模块；设置组件直接使用唯一 `getTextStyleDefaults()` 工厂副本，现有 Store 草稿仍是弹窗内唯一状态。 | 文字样式默认值、设置组件及下载组合 20 tests、typecheck 与 ESLint 通过；全仓确认无删除模块引用。 | FIXED |
| AUDIT-135 | 前后端工厂提示词的双份事实源 | 十组默认提示词同时硬编码于 TypeScript 和 Python，批量普通/JSON 两组已经出现末尾换行差异，网页导入提取提示词也形成单独副本；前端重置和后端新建默认值可能继续漂移，打包无法证明两端使用同一文本。 | 将十组只读出厂提示词收敛到一个严格 JSON 资源；前端常量、后端算法默认值、术语提取和网页导入默认值直接读取该资源，并把资源明确加入 PyInstaller。数据库中的运行时提示词仍是唯一当前事实，不迁移或覆盖已保存设置。 | 后端提示词/网页导入/打包/约束 43 tests、前端提示词/术语 22 tests 与 typecheck 通过；逐项断言十组常量等于共享资源。 | FIXED |
| AUDIT-136 | 前端当前 API 类型的重复手写契约 | 书架任务摘要/标签、Insight 任务状态、OCR 结果、翻译约束和工作流模式已有 OpenAPI 生成类型，前端仍手写第二套相同或更宽松的联合/接口；`TagData` 甚至允许缺失当前必需的 ID/颜色，使组件保留 fallback、测试继续传旧式 `book_count`。书架投影还保存无消费者的章节顺序 revision、章节 ordinal 与页面顺序 revision；API 和管理弹窗又分别使用两种新标签默认颜色。 | 当前 API 结构直接别名生成 schema；删除未消费类型和书架投影镜像字段；标签组件直接消费必需 ID/颜色，不兼容旧字段。API 只发送完整 Tag 命令，唯一业务默认色由书架 Store/常量拥有，所有创建入口一致。 | 类型来源/书架组合两轮共 180 tests、typecheck 和定向 ESLint 通过；全仓确认无 `id || name`、颜色 fallback、`#808080` 或旧 `book_count`。 | FIXED |
| AUDIT-137 | 图片目录投影与翻译状态镜像 | 后端已返回规范化 `logicalSourcePath`，适配器却写入无人读取的 `relativePath`，而目录树只读从未赋值的 `folderPath`，导致文件夹上传在后端重载后丢失目录导航。图片模型同时保存无消费者的 `sourceRevision`/`errorMessage`，并以 `translationStatus` 和 `translationFailed` 两份状态表达同一事实。 | 按当前后端 POSIX 相对路径契约直接拆出 `fileName`/`folderPath`；删除三个无消费者字段和重复失败布尔值，所有缩略图、重试与计数只读取唯一 `translationStatus`。不兼容旧反斜杠路径，也不保留迁移分支。 | 目录适配、目录树、缩略图、选择、Store、翻译流水线及阅读器 12 files / 119 tests、typecheck 与定向 ESLint 全部通过。 | FIXED |
| AUDIT-138 | 全局样式空壳与死动画 | `animations.css` 全局声明六个通用动画和两枚阴影变量，但只有 `spin`/`slideIn` 各有一个组件消费者，其余四个及变量无引用；`base.css` 只有说明文字；`reset.css` 在全局通配规则后又为四类表单控件重复声明 `box-sizing`。这些全局名字还扩大了无必要的样式耦合。 | 将两个真实动画改为组件 owner 内的命名 keyframes，删除死动画文件和空 `base.css`，移除重复 reset 规则；保留并逐项核对有真实消费者的 foundation/semantic/domain token。 | UI primitives/workspace/theme/architecture 4 files / 164 tests、typecheck、Stylelint 与 ESLint 通过。 | FIXED |
| AUDIT-139 | 前端通用工具中的旧字体投影、宽松页码与重复清单 | 当前后端字体目录已经以稳定 ID 和 `displayName` 返回完整资源，但两个字体选择器仍保留旧文件路径到中文名的整套映射，并把目录外值重新插回选项；页码工具还会把小数静默向下取整。任务事件订阅和显示标签、气泡编辑器和全局样式中的修复方式选项也各维护两份。HTML 清洗器则保留生产环境不可能进入的无 DOM 分支。 | 字体选项只消费后端当前目录，删除旧路径映射和目录外回填；页码只接受正整数；事件订阅直接消费标签 owner 导出的当前事件集合；修复方式选项统一为文字样式 owner；删除无 DOM 清洗降级。剪贴板的 HTTP/LAN 浏览器能力降级、严格二值 PNG、资源释放和纯数据深拷贝均有真实运行职责，予以保留。没有旧数据迁移或兼容读取。 | 两轮共 145 项定向测试、typecheck 与 ESLint 通过；逐项对照后端字体、任务事件、页码与修复方法契约。 | FIXED |
| AUDIT-140 | Playwright 运行状态误入源码 | `vue-frontend/test-results/.last-run.json` 只记录本机最近一次测试结果，内容会随每次运行变化，既不是基线也不是可复现契约。 | 删除该生成物；现有 `test-results/*` 忽略规则继续作为唯一边界，不增加脚本或兼容处理。 | Git 跟踪状态与前后端打包输入核对完成。 | FIXED |
| AUDIT-141 | 前端测试中的元测试、重复契约与过期夹具 | 多个单元/属性测试并不执行生产行为，而是读取自身或另一测试文件，检查注释、导入名、函数名、`any`、私有类名或同一路由字符串；这些测试只证明测试文本本身，重复 UI 架构门禁，并继续维护已删除的 `SidebarLayout.mode`、旧 Provider 类型别名以及漏掉当前 `cancelled` 计数的任务夹具。 | 删除 11 个纯源码/元测试文件及散落在行为测试中的自读断言；保留真实行为和生产 owner 边界测试。属性测试改为当前 Sidebar 公共契约，任务/Insight 夹具补齐当前必需字段，Provider 类型断言改为直接共享当前生成类型。 | 测试目录自读搜索只剩架构检查器的专用临时 fixture；前端 24 files / 355 tests 首轮定位两份漂移后，修正项 38 tests 通过，测试目录 ESLint 与 typecheck 通过。 | FIXED |
| AUDIT-142 | 后端测试兼容别名、异常吞噪与过期 LiteLama 钩子 | Stage 4 测试给共享确定性 Provider 套了一层完全无行为且明确标注为 compatibility alias 的 `FakeAlgorithms`；网页导入 Agent 测试用 `except Exception` 把真实失败改成 `None`；LiteLama 回归测试还 patch 已从当前实现删除的 `_cleanup_memory`，导致三项测试在进入被测逻辑前失败。 | Stage 4 直接使用唯一 `DeterministicFakeProvider`，各故障注入子类直接继承它；网页导入测试让异常自然暴露；删除 LiteLama 过期私有方法 patch，不在生产类中恢复任何兼容入口。 | Memory/Insight/Style/Web Import/Stage4 聚焦回归共 95 项中其余 92 项通过并准确暴露 3 项过期钩子；修正后 LiteLama 与 Web Import 6 tests 全部通过，diff check 通过。 | FIXED |
| AUDIT-143 | 前端测试对私有模板与样式实现的重复耦合 | 续写、笔记、设置等行为测试在已经挂载真实组件并验证公共 props、事件和交互后，又读取生产 `.vue` 文本断言内部 import、私有方法名、owner class、CSS token 或断点；这些静态规则已由统一 UI architecture 门禁覆盖，重复断言会让等价重构无故破坏测试。任务进度夹具还漏掉当前必需的 `cancelled` 字段，若干 `UiSelect` 测试则错误检查组件外壳属性而非实际可访问按钮；参考图选择器测试传入从未属于该组件契约的 `bookId`，触发属性泄漏警告。 | 在保留真实渲染、公共 primitive、事件、异步竞态和可访问性行为的前提下，删除组件测试中的私有源码断言；夹具补齐当前任务池字段，按 `UiSelect` 的公开 DOM 边界验证属性，删除过期 `bookId` 测试参数，不在生产组件中增加兼容 prop。 | 定向组件回归与 ESLint 均通过；最终完整前端矩阵 226 files / 1969 tests 全部通过，不再产生属性泄漏或收集警告。 | FIXED |
| AUDIT-144 | 插件持久命令的重试提交边界 | 前一轮只因后端未消费幂等头便删除了配置、默认启用和删除命令的幂等要求，但这三项都会改变持久事实：响应丢失后重试配置会撞上已推进的 revision，重试删除会得到 404，客户端无法判断首次命令是否已提交。仓储公开 DTO 还夹带仅供导出使用、未在 OpenAPI 声明的包路径和校验值；首次读取过期幂等记录时使用普通连接删除但不提交。 | 复用现有 `idempotency_records`，让三条命令在各自同一个短写事务内检查请求哈希、提交事实并记录完整响应；同 key 同输入返回原响应，不同输入返回明确 409，不新增表、迁移或兼容读取。导出单独查询内部包位置，公开 Plugin DTO 只返回当前 OpenAPI 字段；过期记录清理改在可提交的短事务中执行。 | Backend Plugins 与 OpenAPI 60 tests、前端 Plugin API 6 tests、vue-tsc 通过；HTTP 回归覆盖配置/默认/删除重放、冲突码、删除后重放和内部字段不泄漏。 | FIXED |
| AUDIT-145 | Playwright 全页面视觉夹具的当前契约漂移 | 视觉夹具仍保留整份旧书籍 snake_case 对象并把其长度同时当作书架和 Insight 场景开关；编辑气泡 payload 带已删除的 `fontFamily`，却缺当前必需的 `autoFgColor`、`autoBgColor`、`colorConfidence`，严格文档解析因此拒绝加载。示例插件入口也仍是无类名的旧写法。 | 删除未被响应消费的旧书籍对象；书架与 Insight 可用性改为两个明确开关；核心 v2 夹具直接以 OpenAPI 生成类型 `satisfies` 约束，气泡只保留当前 payload，插件入口改为 `plugin.py:Plugin`。移动端新虚拟缩略图列表正确完整展示两页，经逐图核对后更新对应基线。没有在生产代码中增加兼容字段。 | 二值蒙版 1 test、全页面视觉回归 33 tests 全部通过；覆盖五个正式页面、全部主要弹层、暗色、390px、100/500/1000 页 DOM/请求/堆与进程内存边界。 | FIXED |
| AUDIT-146 | 网页导入与插件 Agent 长步骤的任务控制和当前契约 | 网页 AI 导入在 LLM/Firecrawl 多轮循环与指数退避期间不检查任务控制；插件 Agent 只检查取消、不检查暂停，且把控制请求当成普通失败。插件会话窗口又没有 pausing/paused/cancelling 状态，任务中心已暂停时仍显示执行中。网页 Agent 还保留旧嵌套配置适配器、第二份默认提示词、按错误文案猜测重试和从任意前后缀截取 JSON 的兼容解析。 | 两个 Agent 都在模型轮次、工具调用、流块和重试等待的现有安全边界检查暂停/取消，并通过现有 `checkpoint_step` 让出运行步骤；尚未发布的单步恢复时从当前快照重跑，不保存旧中间格式。插件会话直接投影 durable job 的 pausing/paused/cancelling；网页 Agent 改为显式当前构造参数、唯一共享工厂提示词、SDK 异常类型与明确状态码重试，`maxRetries` 按“额外重试次数”执行，删除旧提示词文件和宽松 JSON 外壳截取。 | Web Import/Plugins 定向 82 tests、插件前端 42 tests、vue-tsc 与 OpenAPI 确定性生成通过；暂停和取消均覆盖安全让出，认证错误不重试、内存错误立即失败。 | FIXED |
| AUDIT-147 | 网页导入提示词与文字样式工厂的伪双入口 | 网页导入默认提取提示词仍在前后端各保留一份硬编码，且共享常量通过只在模块导入时调用一次的 `refresh_text_style_runtime_defaults()` 和一组 `global` 赋值伪装成可刷新运行态，形成没有消费者的可变入口。 | 网页导入提示词并入十组唯一共享工厂资源；前后端直接导出同一值。文字样式常量在模块导入时一次性从严格工厂资源初始化，删除空缓存、全局重赋值函数及虚假运行态刷新语义；数据库设置事实不迁移、不回填。 | 提示词、文字样式、网页导入、打包及插件组合 123 tests、前端 typecheck、compileall 与 diff check 通过。 | FIXED |
| AUDIT-148 | 插件 Agent 持久结果截断与会话交接竞态 | 执行模型能看到完整工具结果，但任务事件中的 `debug_result` 会硬截到 1200 字，任务中心和持久日志丢失真实结果。规划或创建持久任务期间又允许删除/过期清理会话；尤其任务已成功入队而会话尚未登记 jobId 的窗口会让接口报 404，留下已运行但前端误判失败的任务。 | 删除持久工具结果截断，完整结果直接进入现有任务事件；显示用 `preview` 仍只承担明确的 UI 摘要职责。规划和入队中的会话不允许删除或 TTL 清理，`_starting_sessions` 一直保持到 jobId、batchId 与运行状态在同一锁内登记完成；不增加恢复表或会话迁移。 | 两类可重复并发回归及完整工具结果回归已加入；Plugins 72 tests、相关组合 80 tests、compileall 与 diff check 通过。 | FIXED |
| AUDIT-149 | 发行打包的静默缺依赖与工作区杂质 | `app.spec` 对 `collect_all`、数据、metadata 和子模块收集全部吞异常，而 PyInstaller 对缺包还会只返回空集合；CPU 依赖集未安装 `accelerate`，spec 却把它当成共同必需项。根目录 25MB README 图片被重复打包，示例插件又原样复制本机 `__pycache__` 和无清单孤儿目录，均不是运行资源。 | 对共同必需模块先用 `find_spec` 显式门禁并让收集失败终止构建；`accelerate` 仅在 GPU 环境实际安装时收集。删除根 README 图片打包；示例插件复制时排除 Python 缓存，并移除没有当前 `plugin.json` 的孤儿目录。没有依赖 fallback 或旧包兼容。 | 打包契约 9 tests、打包/长图组合 26 tests、插件 72 tests 通过；三套示例插件按 v3 打包解析编译成功，真实 CTD/Default 权重分别以 23,402,449/77,018,011 参数按当前 loader 精确装载。 | FIXED |
| AUDIT-150 | 内置检测模型仍夹带训练与版本兼容表面 | CTD/Default 的保留推理代码仍带训练态分支、动态 `eval` 建模、旧 PyTorch buffer/anchor 兼容更新、可选结构和未被当前 checkpoint 使用的层；这扩大了内置权重解析面，也会把错误 checkpoint 解释成另一种模型。 | 按当前随包权重收敛为唯一推理图和白名单层，checkpoint 顶层、YOLO 配置及 state dict 全部精确校验；删除训练头、动态求值、跨版本修补和未使用层，不增加旧权重迁移。Default 同样只保留当前 ResNet34/DBNet 推理结构。 | 真实 CTD/Default 权重以当前 loader 严格加载成功，参数量分别为 23,402,449 与 77,018,011；辅助检测/长图 17 tests 及打包模块存在性契约通过。 | FIXED |
| AUDIT-151 | 共享 Provider 清单的畸形当前集合错误边界 | Provider 清单已拒绝未知字段，但 `capabilityEndpoints` 在类型检查前就调用 `.items()`，非字符串 capability 也可能进入集合操作；损坏的当前工厂 JSON 因而会泄漏偶然的 `AttributeError`/`TypeError`，而不是稳定的清单契约错误。前端清单类型又漏掉后端实际拥有的 endpoints/catalogs，并用 `String()` 隐式接收非字符串。 | 所有集合先验证精确类型再读取，capability 元素必须是当前字符串枚举；前端类型完整表达唯一共享 JSON，并只接受其声明的字符串 Provider 输入。仍保留用户输入大小写/空格规范化，但不映射任何旧 ID。 | 后端 Provider/Transport 35 tests、前端 Provider/Insight 12 tests及完整 typecheck 通过；覆盖旧字段、重复 ID、错误 kind/capability/endpoints 和未知 Provider。 | FIXED |
| AUDIT-152 | 包内资源路径可逃逸资源根目录 | `resource_path()` 名义上解析只读 bundle 相对资源，却接受绝对路径和 `..`；调用者可绕过唯一资源根目录。FreeType 字体加载还先尝试该宽松入口，再回退真正拥有上传字体边界的 `get_font_path()`。 | 包内资源解析只接受非空相对路径，并在 resolve 后验证仍位于当前资源根；FreeType 直接使用统一字体解析器。用户上传字体继续通过现有显式绝对文件分支，不迁移路径、不增加兼容读取。 | 新增路径边界 5 tests，连同打包与文字样式共 16 tests、compileall 通过；覆盖相对资源、绝对路径、目录逃逸和上传字体。 | FIXED |
| AUDIT-153 | 模型修复命令仍携带纯色填充参数 | `lama_mpe`/`litelama` 与恢复原图命令仍在 HTTP、持久 operation、插件钩子和算法入口间携带未消费的 `fillColor`，部分路径甚至会在模型失败后借该值表现成纯色修复；同一命令因此混合两种互斥语义。 | OpenAPI 将修复命令拆成严格判别联合：只有 `solid` 要求颜色，模型修复和恢复原图拒绝该字段；Repository、Worker、核心修复和插件契约保持同一边界，前端只为纯色构造颜色。模型分配/执行失败继续直接失败，不降级为另一种算法。 | Backend Operations/Repair 199 tests、前端 Operations/useBrush 18 tests、OpenAPI 确定性生成与 typecheck 通过。 | FIXED |
| AUDIT-154 | 发布流程可发布错误 revision、漏检生成物并误导失败结果 | 手动触发值直接嵌入 shell，Draft Release 未固定触发 commit，静态前端只检查已跟踪 diff，构建也未执行完整前后端门禁；任意语义预发布标签又只凭 `alpha/beta/rc` 文本猜测。无论构建成功与否，完成摘要都提示用户发布。 | 环境变量接收并严格验证 SemVer；Draft 固定 `${{ github.sha }}`，按是否存在 prerelease 段设置预发布；静态产物用 `git status --porcelain` 捕获新增/删除/修改，CPU 任务执行完整后端和前端门禁。完成摘要显示真实 build result，失败或取消时明确保持草稿。 | Workflow YAML 解析、打包契约 6 tests 与 diff check 通过。 | FIXED |
| AUDIT-155 | 当前存储规范与外围文档仍描述升级/迁移 | Alembic 配置保留大段模板默认项，架构文档仍在若干验收段落描述升级备份、临时数据库复制、旧设置迁移或读时修复；README 的分析层级、QA 权重、检索并行方式、平台发行和路线图也与当前实现不符。 | Alembic 收敛为唯一 foundation 所需配置；所有存储文字统一为空库创建当前 schema、非空库只接受精确 revision，否则舍弃旧数据。README 按当前 2–4/自定义层、80/20 QA、查询变体合并、Windows 自动发行与跨平台源码运行更新，不添加转换工具。 | Storage/Platform 199 tests、配置全文搜索、README 本地引用闭包和 Alembic/YAML 解析通过。 | FIXED |
| AUDIT-156 | 一致性检查脚本无法按文档直接运行 | `python scripts/check_v2_consistency.py` 的模块搜索根落在 `scripts/`，尚未安装成包时会在导入 `src` 前抛出 `ModuleNotFoundError`，连 `--help` 都不可用。 | 脚本从自身位置确定项目根并只在缺失时加入 `sys.path`；不探测旧目录、不修改数据、不增加兼容入口。 | 直接 `--help` 成功，缺少当前数据库时返回明确错误；Stage1 Platform 80 tests 与 scripts compileall 通过。 | FIXED |
| AUDIT-157 | 前端锁文件包含已知漏洞且测试配置声明无效覆盖率入口 | 现有锁文件的 Axios、Vite/Vitest 及 npm registry 上停止维护的 `xlsx@0.18.5` 产生 20 项安全告警；`test:coverage` 和 Vitest coverage 配置却未安装对应 provider，脚本必然失败。 | 更新当前依赖范围，SheetJS 改用官方维护版 0.20.3 tarball；重新解析锁文件，不使用 `force`、override 或审计忽略。删除无人可执行的 coverage 脚本和配置。 | 干净 `npm ci --ignore-scripts` 成功，`npm audit` 为 0；typecheck、表格导入/客户端 17 tests 和生产构建通过。 | FIXED |
| AUDIT-158 | 生成前端夹带无消费者资源与已删除样式空壳规范 | Vite 模板资源 `vite.svg` 和旧 GitHub 图片在源码与生产静态目录均无运行消费者；空 `.stylelintignore` 只留下曾经按文件豁免违规的暗示；UI 规范仍要求已经删除的 `base.css` 空壳。 | 删除两份源图片及其生成副本、空忽略文件和架构扫描器中的特殊分支；规范只列当前真实样式 owner。用 `emptyOutDir` 重新生成完整静态目录。 | UI architecture/default+audit、Stylelint、ESLint、build:check 全部通过；39 个静态文件引用闭包完整且 0 orphan。 | FIXED |
| AUDIT-159 | 非 Windows Launcher 崩溃会遗留 API/Worker | Qt 桌面控制中心可在 Linux/macOS 启动，但 `ChildProcessJob` 在非 Windows 是空实现；正常退出虽显式停止子进程，Launcher 崩溃后子进程却可能继续持有模型和端口。架构文档同时仍声称默认开放局域网，与 GUI 默认仅本机的当前设置相反。 | Windows 保留 Job Object；Launcher 把自身 PID 传给非 Windows 子进程，API/Worker 监视直接父 PID 并在父进程消失时立即退出。新 Launcher 按数据库 PID、角色和规范化数据目录确认旧进程消失后才恢复 epoch，超时拒绝启动，避免新旧执行器重叠。文档统一为三平台退出边界及默认 `127.0.0.1`、用户开启后才绑定 `0.0.0.0`。 | Role boundaries + Platform 97 tests、compileall、diff check 与桌面设置链路核对通过。 | FIXED |
| AUDIT-160 | Studio 文档生成操作使用无意义的 generation 0，并把所有数据库约束错误误报为忙碌 | `studio_generate` 只以文档 revision fencing，没有会话 generation；仓储却写入 `base_generation=0`，与当前 foundation/OpenAPI 的正整数或 null 契约冲突，使所有正常生成请求被 SQLite 拒绝为 423。通用 `IntegrityError` 捕获还会把任意约束损坏伪装成并发忙碌。快速工作区非当前 schema 测试则硬编码了恰好已经成为当前版本的 `2`，形成测试自身漂移。 | 文档生成明确写入 `base_generation=null`，在同一 `BEGIN IMMEDIATE` 事务内用现有 active-operation 查询给出真正的忙碌冲突；删除宽泛完整性异常翻译，让其他当前数据错误直接暴露。非当前 schema 测试从唯一版本常量构造 `current + 1`，只验证拒绝行为，不引入兼容或迁移。 | 原 7 个失败用例全部通过；新增断言确认文档生成受理结果的 generation 为 null；最终完整后端矩阵 858 tests 全部通过。 | FIXED |
| AUDIT-161 | 前端公共图片被测试编译器当作文件模块，提示词工厂契约测试漏项 | Vite 7/Vitest 4 在 Windows 上编译模板中的静态绝对 `/pic/...` 属性时尝试把它解析为 `file:///pic/...`，六个包含页眉或赞助弹窗的测试文件在收集阶段直接失败；共享提示词已新增网页导入唯一工厂项，前端全量相等断言却仍只列九项。 | 公共目录 URL 保持原路径和运行语义，仅改为组件脚本拥有的运行时绑定，避免编译期模块解析；工厂断言补齐 `DEFAULT_WEB_IMPORT_EXTRACTION_PROMPT`，不复制提示词文本。 | 原 7 个失败文件定向复跑为 7 files / 87 tests 全部通过。 | FIXED |
| AUDIT-162 | 最终视觉测试冷启动竞态与已删除配置残留 | Playwright 升级后本机尚无对应 Chromium，首次运行的 34 项均在启动前失败；浏览器补齐后，千页阅读器在并行冷编译时超过默认 5 秒等待，但失败快照已显示内容完整渲染，单独运行所有资源阈值通过。UI 架构脚本还把已删除的空 `.stylelintignore` 留在可选扫描根，因不存在时静默跳过而形成无效配置；架构文档则仍说已删除的 `app.py` 可用于对照。 | 安装与锁文件匹配的测试 Chromium；只把阅读器“进入已加载态”的等待明确为 15 秒，不放宽任何 DOM、请求、堆或进程内存阈值。删除架构脚本的死扫描根；文档明确当前只通过 Git `main` 对照，不恢复 legacy 入口。 | Playwright 34/34 通过；包含 100/500/1000 页资源边界、五个正式页面、主要弹层、暗色及移动端；UI architecture 默认/audit 与最终门禁复跑。 | FIXED |
| AUDIT-163 | 桌面任务 SSE 停止竞态 | 实机启动并停止 API/Worker 时，`QNetworkReply.abort()` 可同步触发 `finished`，回调会先把 `_sse_reply` 清空；`stop()` 随后又通过该共享字段调用 `deleteLater()`，产生 `NoneType` Qt 回调异常。服务进程虽能退出，但桌面日志会出现真实异常。 | `stop()` 在中止前捕获当前 reply，并只在回调尚未接管时清空与释放；不增加状态层、兼容分支或重试。新增同步 `finished` 回归用例。 | 桌面定向 21 tests 通过；生产组件探针重新完成 `starting → running → stopping → stopped`，API `/health` 为 `ok`，无回调异常。 | FIXED |
| AUDIT-164 | 根目录测试缓存未纳入生成物边界 | 最终未跟踪文件复核发现，根目录工具安装产生的 `node_modules/.vite/vitest/results.json` 和独立桌面探针的 `.tmp-desktop-probe/` 数据库会出现在工作区；现有规则只忽略 Vue 子项目依赖、`.tmp/` 内容和 `*.tmp` 文件，未覆盖这两类目录。 | 根 `.gitignore` 明确忽略 `/node_modules/` 与仓库根 `.tmp-*/`；只定义开发生成物边界，不增加运行时代码或旧数据兼容。 | `git check-ignore -v` 验证两类路径均由新规则命中；最终未跟踪列表只保留当前源码、测试、资源与审计台账。 | FIXED |
| AUDIT-165 | 完成性复审台账漏记 `main` 删除项 | 早期依赖图和问题日志虽提到部分已删除入口，但删除明细只规范化了 323 项，漏记 12 个旧文本文件和 3 个旧资源；另有 43 个现存文件虽然状态已写 PASS，结果/证据仍是占位符，导致“全量完成”缺少可机器核验的闭包。 | 逐项重读 43 个现存文件并补齐具体结论/证据；从 Git `main` 原文核对漏记文件的行数与资源字节数，补入删除和资源台账，同时校正依赖图中的 5 个历史行数。只修审计证据，不恢复旧文件、不加入兼容或迁移代码。 | 当前树 1066 项等于 977 个文本文件加 89 个资源，零漏项/重复/交叉；`main` 相对当前缺失的 338 项等于 297 个文本文件加 41 个资源，零漏项/多项/重复/交叉；977 个现存明细均有结论和证据，`PENDING/REVIEWING` 为 0。 | FIXED |
| AUDIT-166 | OpenAPI TypeScript 客户端落后于当前 schema | 最终确定性生成门禁发现 `vue-frontend/src/api/generated/v2.ts` 仍是本轮 OpenAPI 收紧前的版本，缺少提示词事务、严格气泡/Studio/Insight DTO、Operation 错误与若干当前路由变化；手写前端恰好未触发类型失败，不能据此判定生成闭包完整。 | 直接从当前 `openapi/v2.yaml` 重新生成唯一 TypeScript 客户端，不手工修改生成代码、不保留旧 DTO；随后再做第二次生成哈希比较。 | 连续两次生成 SHA-256 均为 `05BDCE37310F43BA1A5B921FD6B1453F6CCC3C922F0DF8D4A3C500579AD49D6E`；更新后前端 226 files / 1969 tests、typecheck、ESLint、生产构建与 Playwright 34/34 通过，静态产物 39/39 可达、0 orphan。 | FIXED |
| AUDIT-167 | 当前任务状态与通用领域小工具仍有重复 owner | 后端若干领域分别维护相同的 job/operation/render 状态元组、逻辑路径去重、自然排序、可选字符串和图片 RGB 转换；前端任务中心、Insight 与翻译又各写一组非终态状态。重复值已开始出现投影差异风险，统一导出后还有三个测试整模块 mock 漏掉新导出而在收集阶段失败。 | 状态集合归并到唯一 schema/API owner，小工具只在已有两个以上真实消费者时下沉到所属公共模块；业务域直接复用。三个测试改为保留真实常量的部分 mock，不在生产代码增加测试兼容导出。 | Python AST 重复体扫描只剩领域构造器和微型本地校验；前端 3 个漂移文件 29 tests、最终 226 files / 1970 tests，后端 858 tests 全部通过。 | FIXED |
| AUDIT-168 | Studio 诊断草稿被前端解码器拒绝，OpenAPI 声明伪约束 | 后端有意允许保存带非法优先级、间隔或触发时机的当前草稿，让诊断器展示并修复；前端通用整数解码却隐含 `minimum=0`，触发时机也一度被收窄为枚举，导致诊断页面根本无法加载该草稿。Studio 创建标题、会话标题、消息长度和附件数量还在 OpenAPI 中写了后端从未执行的上限。 | 整数解码只在调用方明确传入时才施加下限，草稿触发时机保持字符串并由诊断/patch 边界判断；删除所有后端不存在的 Studio 上限。继续严格拒绝未知字段，不增加旧格式读取。 | 新增负值/非法触发时机草稿装载回归；Studio/OpenAPI 定向 48 tests、前端 60 tests、类型检查、完整前后端矩阵与双生成通过。 | FIXED |
| AUDIT-169 | 直接 CLI 启动仍默认暴露局域网 | GUI 默认只监听 `127.0.0.1`，架构文档也已采用本机默认，但统一命令入口的 `--host` 仍默认 `0.0.0.0`，文档末尾还残留“默认开放局域网”的旧描述；同一产品因启动入口不同而拥有两套网络默认值。 | CLI 默认改为 `127.0.0.1`；只有用户显式传入 `0.0.0.0` 或在 GUI 开启局域网访问时才对外绑定。同步文档与打包契约，不添加自动探测或迁移设置。 | 桌面设置、角色边界和打包契约 28 tests 通过；全文搜索确认无相反默认描述。 | FIXED |
| AUDIT-170 | 隐式参数扩展面与前端提示词伪上限 | 检测器构造、注册、长图包装、原始检测和后处理连续多层接受/转发任意 `**kwargs`，当前调用实际只有三个辅助 YOLO 参数；提示词校验和翻译提示词选择各携带一个未读取参数。前端导入提示词又单独限制 200,000 字，而后端当前契约允许同一内容正常保存；网页 URL 的真实 8,000 字后端边界反而未写入输入 schema。 | 检测链改为三个明确参数，内置模型拒绝未知构造/检测字段，Yolo 真正使用的原始置信度参数继续显式保留；删除两个未用参数和前端伪上限，并把实际 URL 安全边界写入两个输入命令 schema。没有泛型适配层或旧调用兼容。 | 检测/翻译 106 tests、设置/网页导入/打包 110 tests、前端提示词 28 tests、完整矩阵、Pyflakes、typecheck 与 OpenAPI 双生成通过。 | FIXED |
| AUDIT-171 | Playwright 点文件逃出生成物忽略边界 | `.gitignore` 使用 `test-results/*`，该模式不匹配目录内的 `.last-run.json`；每次完整视觉测试后都会新增未跟踪状态文件，使“干净生成物闭包”结论失真。 | 改为忽略完整 `test-results/` 目录，并删除本轮测试生成、仅含通过状态的 `.last-run.json`。 | `git check-ignore` 与最终未跟踪集合复核通过；Playwright 34/34，五页面及 100/500/1000 页资源边界全部通过。 | FIXED |
| AUDIT-172 | 审计映射状态同步的终端编码损坏 | 最终台账一致性检查发现，两份派生映射在机械同步时把部分中文状态写成了问号占位符；主台账、生产源码和测试结果未受影响，但派生审阅结果已不可直接核验。 | 不再经终端传递中文状态，直接按 `current-files.tsv` 的稳定英文状态重新同步两份映射，并校正一次生成静态文件行号漂移。 | 两份映射中问号占位符为 0；977 文本、89 当前资源、297 删除文本、41 删除资源、273 功能契约及 172 条发现全部通过唯一性、存在性、行号和集合闭包校验。 | FIXED |
| AUDIT-173 | 普通翻译空自定义 Base URL 未采用服务商默认值 | SiliconFlow 等内置服务商允许自定义地址留空并由清单提供默认地址；任务冻结配置把留空表示为 `""`，普通翻译的单条和批量请求却原样传入只接受 `None` 或非空字符串的严格请求 DTO，因此在任何 HTTP 请求发出前失败。术语提取和高质量翻译已使用正确语义。 | 保留严格 DTO，只在普通翻译的两个请求边界将空字符串规范化为 `None`，继续由唯一 Provider 清单解析默认地址；不增加兼容读取、迁移或降级。 | 故障语义 34 tests、Stage4/同步与异步 Provider 114 tests 全部通过；覆盖单条、批量、显式自定义地址、Sakura、本地模型和高质量翻译相邻契约。 | FIXED |
| AUDIT-174 | Studio 空自定义 Base URL 未采用服务商默认值 | Studio 的冻结 Provider 配置同样使用 `""` 表示未填写自定义地址，`_provider_config` 却把空串传给严格请求 DTO，角色工坊生成、聊天或摘要会在请求发出前失败。 | 保留严格 DTO，只在 Studio 当前配置转请求的唯一边界把精确空串改为 `None`；空白字符串继续按非法配置拒绝，不增加公共兼容层或迁移。 | Studio 定向测试覆盖非流式请求和空地址默认语义；完整后端测试通过。 | FIXED |
| AUDIT-175 | 审阅主台账再次出现编码损坏 | 本轮复核发现 `current-files.tsv` 有 41 行、112 处连续问号，涉及 AUDIT-167 至 AUDIT-171 的结果和证据；这与 AUDIT-172 的“已清零”结论冲突，说明后续机械同步再次破坏了审阅结果。 | 按每个文件的真实职责和对应 finding 逐行恢复 41 行，不经终端批量重写中文；保留稳定英文状态列，并增加连续问号、TSV 列完整性和状态集合检查。 | 978 行（含表头）可由 TSV 解析；977 条数据行的状态均为 `PASS`、`FIXED` 或 `GENERATED-VERIFIED`，路径与证据非空，连续问号为 0。 | FIXED |
